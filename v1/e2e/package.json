{
  "name": "domo-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --project=user-smoke --project=admin-smoke",
    "test:user": "playwright test --project=user-smoke",
    "test:admin": "playwright test --project=admin-smoke",
    "test:cross": "playwright test --project=cross-app",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "dotenv": "^17.4.2",
    "otplib": "^13.4.0"
  }
}

---
template: design
version: 1.0
feature: ml-feed-collaborative-filtering
phase: 9 / K-1
date: 2026-05-05
author: itpe-ince (Claude Sonnet 4.6)
project: domo
project_version: v1
parent_plan: domo-phase9-roadmap.plan.md
dependency: domo-phase9-L-A.design.md
status: Draft
---

# Phase 9 K-1 Design — Collaborative Filtering 피드 v2 (Critical Path)

> **Summary**: L-A에서 구축한 pgvector 임베딩 인프라 (user_embeddings, post_embeddings) 위에서
> Matrix Factorization 기반 협업 필터링을 구현하고, 기존 룰 기반 피드 스코어링을 ML 개인화 피드로 교체한다.
> A/B 분기(PostHog feature flag), Redis 5분 캐시, Cold User fallback을 포함한 production-ready 설계.

---

## 1. 목표 & Acceptance Criteria

### 목표

| # | 목표 | 이유 |
|---|------|------|
| 1 | Matrix Factorization 모델 학습 파이프라인 구축 | 50K+ behavioral history → Precision@10 ≥ 0.15 달성 |
| 2 | `/api/feed` ML 스코어 기반 정렬 (v2) | 기존 chronological/rule-based 피드 대체, CTR ↑ |
| 3 | Redis 5분 TTL 사용자별 추천 캐시 | Feed API p95 ≤ 200ms (cache hit) 목표 |
| 4 | Cold User fallback 보장 | interaction < 5건 사용자 → 인기/최신 피드 자동 전환 |
| 5 | PostHog feature flag A/B 분기 | v1(기존) vs v2(ML) 통계적 비교 준비 (K-8과 연계) |
| 6 | Mock 모드 graceful fallback | ML 라이브러리 미설치·모델 미준비 시에도 서비스 정상 동작 |

### Acceptance Criteria

- [ ] alembic 0073 적용 후 `user_post_interactions`, `ml_models` 테이블 생성 확인
- [ ] `ml_feed_training.py` — 90일 implicit feedback 수집 → MF 모델 학습 → `ml_models` row INSERT 확인
- [ ] `ml_feed_inference.py` — `get_recommendations(user_id)` Redis cache hit ≤ 200ms, cold DB ≤ 1s
- [ ] `GET /api/feed?algo=v2` → ML 스코어 정렬 응답 (포스트 리스트 형식 동일)
- [ ] `GET /api/feed?algo=v1` → 기존 feed_scoring 동작 (회귀 없음)
- [ ] interaction < 5건 사용자 → fallback chronological feed 자동 적용
- [ ] ML 라이브러리 미설치 시 → WARNING 로그 + fallback, 서버 재시작 불필요
- [ ] `ml_training_worker` cron 등록 (20번째 worker, `ML_TRAINING_WORKER_ENABLED` guard)
- [ ] unit tests 통과: `test_ml_feed_inference.py`, `test_ml_feed_training.py`
- [ ] integration tests 통과: `test_feed_api_v2.py`

### KPIs

| 지표 | 기준 | 측정 방법 |
|------|------|----------|
| Feed CTR | Phase 8 baseline 대비 ≥ 15% ↑ | K-8 A/B 테스트 (PostHog) |
| Precision@10 | ≥ 0.15 | offline evaluation (test split) |
| Feed API p95 latency | ≤ 200ms (cache hit), ≤ 1s (cold) | OpenTelemetry X-Ray |
| Redis cache hit rate | ≥ 70% | Prometheus cache_hit_total |
| A/B 통계적 유의성 | p-value < 0.05 (7일 이상 운영 후) | PostHog experiment |
| Cold user fallback 비율 | ≤ 30% (사용자 engagement 증가 목표) | Prometheus ml_cold_user_total |

---

## 2. Architecture

### 전체 데이터 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│                     TRAINING PIPELINE (일 1회 새벽 3:00 UTC)     │
│                                                                  │
│  PostgreSQL                                                      │
│  ┌──────────────────┐    collect_interactions()                  │
│  │ user_post_       │ ──────────────────────────┐               │
│  │ interactions     │                            ▼               │
│  │ (90일 implicit   │              ┌─────────────────────────┐   │
│  │  feedback)       │              │  ml_feed_training.py    │   │
│  └──────────────────┘              │  train_mf_model()       │   │
│  ┌──────────────────┐              │  (numpy/scipy MF)       │   │
│  │ user_embeddings  │ ──────────── │                         │   │
│  │ (L-A pgvector)   │              │  사용자 latent factor   │   │
│  └──────────────────┘              │  아이템 latent factor   │   │
│  ┌──────────────────┐              └──────────┬──────────────┘   │
│  │ post_embeddings  │                         │ save_model_      │
│  │ (L-A pgvector)   │                         │ artifacts()      │
│  └──────────────────┘                         ▼                  │
│                                  ┌─────────────────────────┐    │
│                                  │   ml_models 테이블       │    │
│                                  │   status='active'        │    │
│                                  │   params JSONB (factors) │    │
│                                  └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     INFERENCE PIPELINE (요청 시)                  │
│                                                                  │
│  Client                                                         │
│    │  GET /api/feed?algo=v2 (또는 PostHog flag ON)               │
│    ▼                                                             │
│  posts.py router                                                │
│    │  PostHog feature flag 조회 (algo=auto 시)                   │
│    ├─ v2 ON ──────────────────────────────────────────────────┐ │
│    │                                                           │ │
│    │                    ml_feed_inference.py                   │ │
│    │                    get_recommendations(user_id, top_k=20) │ │
│    │                    │                                      │ │
│    │                    ├─ Redis 조회 (ml_feed:{user_id})       │ │
│    │                    │   HIT ──────────────────────────────▶│ │
│    │                    │   MISS                               │ │
│    │                    │    │                                 │ │
│    │                    │    ├─ interaction_count < 5?          │ │
│    │                    │    │   YES → Cold User Fallback       │ │
│    │                    │    │          chronological feed      │ │
│    │                    │    │                                  │ │
│    │                    │    ├─ ml_models active 존재?          │ │
│    │                    │    │   NO  → Mock Fallback            │ │
│    │                    │    │          chronological feed      │ │
│    │                    │    │                                  │ │
│    │                    │    └─ MF 점수 계산                    │ │
│    │                    │        user_factor @ item_factors.T   │ │
│    │                    │        + pgvector cosine 보정         │ │
│    │                    │        → top_k 정렬                   │ │
│    │                    │        → Redis SET (5분 TTL)          │ │
│    │                    └──────────────────────────────────────▶│ │
│    └─ v1 OFF ─── 기존 feed_scoring (chronological/rule)  ──────▶│ │
│                                                                  │
│    응답: PostOut[] (형식 동일)                                    │
└─────────────────────────────────────────────────────────────────┘
```

### L-A 임베딩과의 통합

```
L-A 구축 인프라                  K-1 활용 방식
──────────────────────────────────────────────────────────────────
user_embeddings (vector(128))  → MF 초기화 seed (cold start 보정)
post_embeddings (vector(128))  → pgvector cosine similarity 보정 점수
behavioral_history (50K+)      → user_post_interactions 집계 소스
embedding_model.encode()       → K-1 사용 안 함 (MF는 별도 학습)
ivfflat 인덱스 (<=>)           → "사용자와 유사한 포스트" ANN 보정 쿼리
```

MF 점수와 pgvector cosine 보정 점수를 결합한 최종 스코어:

```
final_score = 0.7 * mf_score + 0.3 * cosine_similarity
```

---

## 3. Database Schema (alembic 0073)

파일: `v1/backend/alembic/versions/0073_ml_feed_v2.py`

> **alembic 번호 결정**: K-1=0073 (Critical Path, 먼저 진입), K-3=0078, K-5=0079.
> plan.md K-3 캡션 항목에서 0073을 언급했으나 K-1이 Critical Path이므로 0073을 K-1에 양도.

```python
"""alembic 0073 — ML Feed v2 (user_post_interactions + ml_models)

Phase 9 K-1: Collaborative Filtering 피드 v2 사전 조건.
- user_post_interactions: implicit feedback 수집 (view/like/comment/sponsor/click)
- ml_models: 학습된 MF 모델 메타데이터 + params JSONB 저장

Depends: 0072_cohort_alert_history (L-F), 0066_ml_embeddings (L-A)
"""
from __future__ import annotations

from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = "0073"
down_revision = "0072"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # user_post_interactions — implicit feedback 원본 로그
    op.create_table(
        "user_post_interactions",
        sa.Column("id", postgresql.UUID(as_uuid=True), primary_key=True,
                  server_default=sa.text("gen_random_uuid()")),
        sa.Column("user_id", postgresql.UUID(as_uuid=True),
                  sa.ForeignKey("users.id", ondelete="CASCADE"), nullable=False),
        sa.Column("post_id", postgresql.UUID(as_uuid=True),
                  sa.ForeignKey("posts.id", ondelete="CASCADE"), nullable=False),
        sa.Column(
            "interaction_type",
            sa.Enum("view", "like", "comment", "sponsor", "click",
                    name="interaction_type_enum"),
            nullable=False,
        ),
        sa.Column("weight", sa.Float, nullable=False, server_default="1.0"),
        sa.Column("created_at", sa.DateTime(timezone=True),
                  nullable=False, server_default=sa.text("now()")),
    )

    # 인덱스: 사용자별 최신 순 조회 (학습 데이터 수집)
    op.create_index(
        "ix_upi_user_id_created_at",
        "user_post_interactions",
        ["user_id", sa.text("created_at DESC")],
    )
    # 인덱스: 포스트별 최신 순 조회 (인기도 집계)
    op.create_index(
        "ix_upi_post_id_created_at",
        "user_post_interactions",
        ["post_id", sa.text("created_at DESC")],
    )
    # 인덱스: 학습 데이터 기간 필터 (90일 슬라이딩 윈도우)
    op.create_index(
        "ix_upi_created_at",
        "user_post_interactions",
        ["created_at"],
    )

    # ml_models — 학습된 모델 메타데이터
    op.create_table(
        "ml_models",
        sa.Column("id", postgresql.UUID(as_uuid=True), primary_key=True,
                  server_default=sa.text("gen_random_uuid()")),
        sa.Column(
            "model_type",
            sa.Enum("mf", "two_tower", name="ml_model_type_enum"),
            nullable=False,
            server_default="mf",
        ),
        sa.Column("version", sa.String(50), nullable=False),
        sa.Column("trained_at", sa.DateTime(timezone=True),
                  nullable=False, server_default=sa.text("now()")),
        sa.Column("params", postgresql.JSONB, nullable=True),
        sa.Column(
            "status",
            sa.Enum("training", "active", "archived", name="ml_model_status_enum"),
            nullable=False,
            server_default="training",
        ),
    )
    # 활성 모델 빠른 조회 인덱스
    op.create_index(
        "ix_ml_models_status_trained_at",
        "ml_models",
        ["status", sa.text("trained_at DESC")],
    )


def downgrade() -> None:
    op.drop_index("ix_ml_models_status_trained_at", table_name="ml_models")
    op.drop_table("ml_models")
    op.execute("DROP TYPE IF EXISTS ml_model_status_enum")
    op.execute("DROP TYPE IF EXISTS ml_model_type_enum")

    op.drop_index("ix_upi_created_at", table_name="user_post_interactions")
    op.drop_index("ix_upi_post_id_created_at", table_name="user_post_interactions")
    op.drop_index("ix_upi_user_id_created_at", table_name="user_post_interactions")
    op.drop_table("user_post_interactions")
    op.execute("DROP TYPE IF EXISTS interaction_type_enum")
```

### 스키마 요약

| 테이블 | 컬럼 | 타입 | 설명 |
|--------|------|------|------|
| `user_post_interactions` | `id` | UUID PK | gen_random_uuid() |
| | `user_id` | UUID FK | users.id, CASCADE |
| | `post_id` | UUID FK | posts.id, CASCADE |
| | `interaction_type` | ENUM | view/like/comment/sponsor/click |
| | `weight` | FLOAT | implicit feedback 가중치 (기본 1.0) |
| | `created_at` | TIMESTAMPTZ | 이벤트 발생 시각 |
| `ml_models` | `id` | UUID PK | gen_random_uuid() |
| | `model_type` | ENUM | mf / two_tower |
| | `version` | VARCHAR(50) | 버전 식별자 (날짜 기반) |
| | `trained_at` | TIMESTAMPTZ | 학습 완료 시각 |
| | `params` | JSONB | user_factors, item_factors (직렬화) |
| | `status` | ENUM | training / active / archived |

**Interaction Weight 기준표**:

| interaction_type | weight | 근거 |
|-----------------|--------|------|
| view (≥3초) | 1.0 | 기본 관심 표현 |
| click | 1.5 | 명시적 행동 |
| like | 3.0 | 높은 engagement |
| comment | 4.0 | 가장 강한 engagement |
| sponsor | 5.0 | 최고 강도 (후원) |

---

## 4. Service Layer

### 4-1. `app/services/ml_feed_training.py`

```python
"""ML Feed v2 학습 파이프라인 — Phase 9 K-1 (Collaborative Filtering).

OQ 결정:
  - 모델: Matrix Factorization (implicit 라이브러리 또는 numpy/scipy)
  - 학습 데이터: 최근 90일 implicit feedback (weight 합산)
  - 학습 주기: 일 1회 batch (새벽 3:00 UTC, 20번째 cron worker)
  - 모델 저장: ml_models 테이블 params JSONB (user_factors, item_factors)

Mock 모드:
  - implicit 라이브러리 미설치 → numpy random matrix 반환 + WARNING 로그
  - 빈 interaction 데이터 → 학습 스킵 + WARNING 로그
"""
from __future__ import annotations

import asyncio
import json
import logging
import os
from datetime import datetime, timedelta, timezone
from typing import Any

log = logging.getLogger(__name__)

_TRAINING_DAYS = int(os.getenv("ML_TRAINING_DAYS", "90"))
_MF_FACTORS = int(os.getenv("ML_MF_FACTORS", "50"))
_MF_ITERATIONS = int(os.getenv("ML_MF_ITERATIONS", "20"))
_MIN_INTERACTIONS = int(os.getenv("ML_MIN_INTERACTIONS", "100"))


# ──────────────────────────────────────────────────────────────────────────────
# 학습 데이터 수집
# ──────────────────────────────────────────────────────────────────────────────

async def collect_interactions(db, days: int = _TRAINING_DAYS) -> dict[str, Any]:
    """최근 N일 implicit feedback 집계.

    반환:
        {
            "user_ids": [uuid_str, ...],      # 정렬된 고유 사용자 ID
            "post_ids": [uuid_str, ...],      # 정렬된 고유 포스트 ID
            "user_idx": {uuid_str: int},      # user → matrix row index
            "post_idx": {uuid_str: int},      # post → matrix col index
            "interactions": [(u_idx, p_idx, weight_sum), ...],
        }
    """
    from sqlalchemy import text

    cutoff = datetime.now(timezone.utc) - timedelta(days=days)

    result = await db.execute(
        text("""
            SELECT user_id::text, post_id::text, SUM(weight) AS total_weight
            FROM user_post_interactions
            WHERE created_at >= :cutoff
            GROUP BY user_id, post_id
            HAVING SUM(weight) > 0
            ORDER BY user_id, post_id
        """),
        {"cutoff": cutoff},
    )
    rows = result.fetchall()

    if not rows:
        log.warning("collect_interactions: 상호작용 데이터 없음 (cutoff=%s)", cutoff)
        return {}

    # 고유 사용자/포스트 인덱스 생성
    user_ids = sorted({r.user_id for r in rows})
    post_ids = sorted({r.post_id for r in rows})
    user_idx = {uid: i for i, uid in enumerate(user_ids)}
    post_idx = {pid: i for i, pid in enumerate(post_ids)}

    interactions = [
        (user_idx[r.user_id], post_idx[r.post_id], float(r.total_weight))
        for r in rows
    ]

    log.info(
        "collect_interactions: %d users, %d posts, %d pairs (cutoff=%s)",
        len(user_ids), len(post_ids), len(interactions), cutoff,
    )
    return {
        "user_ids": user_ids,
        "post_ids": post_ids,
        "user_idx": user_idx,
        "post_idx": post_idx,
        "interactions": interactions,
    }


# ──────────────────────────────────────────────────────────────────────────────
# Matrix Factorization 학습
# ──────────────────────────────────────────────────────────────────────────────

def train_mf_model(
    interactions: dict[str, Any],
    n_factors: int = _MF_FACTORS,
    n_iterations: int = _MF_ITERATIONS,
) -> dict[str, Any] | None:
    """Matrix Factorization 학습.

    1순위: implicit 라이브러리 (ALS 알고리즘, 성능 최적)
    2순위: scipy sparse + numpy SVD (fallback)
    Mock: 데이터 없거나 라이브러리 미설치 → None 반환

    반환: {"user_factors": [[float...]], "item_factors": [[float...]]}
           또는 None (학습 불가)
    """
    if not interactions:
        log.warning("train_mf_model: interaction 데이터 없음 — 학습 스킵")
        return None

    n_users = len(interactions["user_ids"])
    n_posts = len(interactions["post_ids"])
    pairs = interactions["interactions"]

    if len(pairs) < _MIN_INTERACTIONS:
        log.warning(
            "train_mf_model: 상호작용 %d건 < 최소 %d건 — 학습 스킵",
            len(pairs), _MIN_INTERACTIONS,
        )
        return None

    # implicit 라이브러리 시도 (ALS)
    try:
        import implicit  # type: ignore[import]
        import scipy.sparse as sp
        import numpy as np

        log.info("train_mf_model: implicit ALS 학습 시작 (users=%d, posts=%d)", n_users, n_posts)

        # CSR matrix: user × post
        rows_idx = [p[0] for p in pairs]
        cols_idx = [p[1] for p in pairs]
        data = [p[2] for p in pairs]
        user_post_matrix = sp.csr_matrix((data, (rows_idx, cols_idx)), shape=(n_users, n_posts))

        model = implicit.als.AlternatingLeastSquares(
            factors=n_factors,
            iterations=n_iterations,
            use_gpu=False,
        )
        model.fit(user_post_matrix)

        user_factors = model.user_factors.tolist()
        item_factors = model.item_factors.tolist()

        log.info("train_mf_model: ALS 학습 완료 (factors=%d, iter=%d)", n_factors, n_iterations)
        return {"user_factors": user_factors, "item_factors": item_factors}

    except ImportError:
        log.warning("train_mf_model: implicit 라이브러리 미설치 — scipy SVD fallback 시도")

    # scipy SVD fallback
    try:
        import scipy.sparse as sp
        import numpy as np
        from scipy.sparse.linalg import svds

        rows_idx = [p[0] for p in pairs]
        cols_idx = [p[1] for p in pairs]
        data = [p[2] for p in pairs]
        mat = sp.csr_matrix((data, (rows_idx, cols_idx)), shape=(n_users, n_posts), dtype=float)

        k = min(n_factors, min(n_users, n_posts) - 1)
        if k < 1:
            log.warning("train_mf_model: SVD k=%d < 1 — 학습 불가", k)
            return None

        u, s, vt = svds(mat, k=k)
        user_factors = (u * s).tolist()
        item_factors = vt.T.tolist()

        log.info("train_mf_model: scipy SVD 학습 완료 (k=%d)", k)
        return {"user_factors": user_factors, "item_factors": item_factors}

    except ImportError:
        log.warning("train_mf_model: scipy 미설치 — numpy random mock 사용")

    # numpy mock (최후 수단 — 점수 의미 없음, fallback용)
    try:
        import numpy as np
        user_factors = np.random.randn(n_users, n_factors).tolist()
        item_factors = np.random.randn(n_posts, n_factors).tolist()
        log.warning("train_mf_model: MOCK MODE (random factors) — 추천 품질 없음")
        return {"user_factors": user_factors, "item_factors": item_factors}
    except ImportError:
        log.error("train_mf_model: numpy도 미설치 — 학습 완전 불가")
        return None


# ──────────────────────────────────────────────────────────────────────────────
# 모델 저장
# ──────────────────────────────────────────────────────────────────────────────

async def save_model_artifacts(
    db,
    model_result: dict[str, Any],
    interactions: dict[str, Any],
    version: str | None = None,
) -> str | None:
    """학습된 모델을 ml_models 테이블에 저장.

    기존 active 모델 → archived 처리 후 새 모델 active로 등록.
    반환: 새 모델 id (str) 또는 None (저장 실패)
    """
    from sqlalchemy import text

    if not version:
        version = datetime.now(timezone.utc).strftime("mf-%Y%m%d")

    now = datetime.now(timezone.utc)

    # params: user_factors, item_factors, user_id 매핑 포함
    params = {
        "user_factors": model_result["user_factors"],
        "item_factors": model_result["item_factors"],
        "user_ids": interactions.get("user_ids", []),
        "post_ids": interactions.get("post_ids", []),
    }

    try:
        # 기존 active 모델 archived 처리
        await db.execute(
            text("UPDATE ml_models SET status = 'archived' WHERE status = 'active'")
        )

        # 새 모델 INSERT
        result = await db.execute(
            text("""
                INSERT INTO ml_models (model_type, version, trained_at, params, status)
                VALUES ('mf', :ver, :ts, :params::jsonb, 'active')
                RETURNING id
            """),
            {"ver": version, "ts": now, "params": json.dumps(params)},
        )
        model_id = str(result.scalar_one())
        await db.commit()

        log.info("save_model_artifacts: 모델 저장 완료 (id=%s, version=%s)", model_id, version)
        return model_id

    except Exception as exc:  # noqa: BLE001
        log.error("save_model_artifacts: 저장 실패 — %s", exc)
        await db.rollback()
        return None


# ──────────────────────────────────────────────────────────────────────────────
# Cron loop — 일 1회 (새벽 3:00 UTC)
# ──────────────────────────────────────────────────────────────────────────────

async def ml_training_cron_loop(
    interval_seconds: int | None = None,
) -> None:
    """ML 학습 cron worker — R-5 격리 패턴.

    interval_seconds: 기본 86400 (24h). env ML_TRAINING_INTERVAL_SECONDS 우선.
    """
    from app.db.session import AsyncSessionLocal

    _interval = interval_seconds or int(
        os.getenv("ML_TRAINING_INTERVAL_SECONDS", "86400")
    )

    log.info("ml_training_cron_loop: 시작 (interval=%ds)", _interval)
    while True:
        await asyncio.sleep(_interval)
        async with AsyncSessionLocal() as db:
            try:
                log.info("ml_training_cron_loop: 학습 시작")
                interactions = await collect_interactions(db)
                if not interactions:
                    log.warning("ml_training_cron_loop: 데이터 없음 — 스킵")
                    continue

                model_result = train_mf_model(interactions)
                if model_result is None:
                    log.warning("ml_training_cron_loop: 학습 실패 — 스킵")
                    continue

                model_id = await save_model_artifacts(db, model_result, interactions)
                log.info("ml_training_cron_loop: 완료 (model_id=%s)", model_id)

            except Exception as exc:  # noqa: BLE001
                log.warning("ml_training_cron_loop: 오류 — %s", exc)
```

### 4-2. `app/services/ml_feed_inference.py`

```python
"""ML Feed v2 추론 서비스 — Phase 9 K-1 (Collaborative Filtering).

추론 흐름:
  1. Redis cache 조회 (ml_feed:{user_id}) → HIT → 즉시 반환
  2. MISS → interaction count 확인
     - < 5건 → Cold User fallback (chronological)
  3. ml_models active 조회
     - 없음 → Mock fallback (chronological)
  4. MF 점수 계산 (user_factor @ item_factors.T)
  5. pgvector cosine 보정 (0.3 가중치)
  6. 최종 정렬 → Redis SET (5분 TTL) → 반환

성능 목표:
  - cache hit: ≤ 200ms
  - cold (DB 직접): ≤ 1s
"""
from __future__ import annotations

import json
import logging
import os
from typing import Any

from app.services.cache import cache

log = logging.getLogger(__name__)

_CACHE_TTL = int(os.getenv("ML_FEED_CACHE_TTL_SECONDS", "300"))  # 5분
_COLD_USER_THRESHOLD = int(os.getenv("ML_COLD_USER_THRESHOLD", "5"))
_DEFAULT_TOP_K = int(os.getenv("ML_FEED_TOP_K", "20"))


def _cache_key(user_id: str) -> str:
    return f"ml_feed:{user_id}"


# ──────────────────────────────────────────────────────────────────────────────
# 메인 추론 함수
# ──────────────────────────────────────────────────────────────────────────────

async def get_recommendations(
    db,
    user_id: str,
    top_k: int = _DEFAULT_TOP_K,
) -> list[str]:
    """사용자 맞춤 포스트 ID 리스트 반환 (ML 스코어 정렬).

    반환: post_id (str) 리스트, top_k개.
    fallback 시: chronological post_id 리스트 (동일 형식).
    """
    key = _cache_key(user_id)

    # 1. Redis cache hit 확인
    cached = await cache.get_json(key, prefix="ml_feed")
    if cached is not None:
        log.debug("get_recommendations: cache HIT (user=%s)", user_id)
        return cached  # type: ignore[return-value]

    # 2. interaction count 확인 (cold user 판별)
    interaction_count = await _get_interaction_count(db, user_id)
    if interaction_count < _COLD_USER_THRESHOLD:
        log.info(
            "get_recommendations: cold user (user=%s, interactions=%d) → fallback",
            user_id, interaction_count,
        )
        return await _chronological_fallback(db, top_k)

    # 3. 활성 모델 조회
    model_params = await _load_active_model(db)
    if model_params is None:
        log.warning("get_recommendations: 활성 모델 없음 → fallback (user=%s)", user_id)
        return await _chronological_fallback(db, top_k)

    # 4. MF 점수 계산
    try:
        post_ids = await _compute_mf_scores(db, user_id, model_params, top_k)
    except Exception as exc:  # noqa: BLE001
        log.warning("get_recommendations: MF 점수 계산 실패 (%s) → fallback", exc)
        return await _chronological_fallback(db, top_k)

    # 5. Redis SET (5분 TTL)
    await cache.set_json(key, post_ids, ttl_seconds=_CACHE_TTL, prefix="ml_feed")

    return post_ids


async def _get_interaction_count(db, user_id: str) -> int:
    """사용자의 총 상호작용 건수 조회."""
    from sqlalchemy import text

    result = await db.execute(
        text("SELECT COUNT(*) FROM user_post_interactions WHERE user_id = :uid"),
        {"uid": user_id},
    )
    return int(result.scalar_one() or 0)


async def _load_active_model(db) -> dict[str, Any] | None:
    """ml_models에서 가장 최신 active 모델의 params 반환."""
    from sqlalchemy import text

    result = await db.execute(
        text("""
            SELECT params FROM ml_models
            WHERE status = 'active'
            ORDER BY trained_at DESC
            LIMIT 1
        """)
    )
    row = result.fetchone()
    if row is None or not row.params:
        return None
    return row.params if isinstance(row.params, dict) else json.loads(row.params)


async def _compute_mf_scores(
    db,
    user_id: str,
    model_params: dict[str, Any],
    top_k: int,
) -> list[str]:
    """MF 점수 + pgvector cosine 보정으로 최종 포스트 ID 리스트 계산."""
    from sqlalchemy import text

    try:
        import numpy as np
    except ImportError:
        log.warning("_compute_mf_scores: numpy 미설치 → fallback")
        return await _chronological_fallback(db, top_k)

    user_ids: list[str] = model_params.get("user_ids", [])
    post_ids: list[str] = model_params.get("post_ids", [])
    user_factors = np.array(model_params.get("user_factors", []))
    item_factors = np.array(model_params.get("item_factors", []))

    if user_id not in user_ids or len(user_factors) == 0 or len(item_factors) == 0:
        log.info("_compute_mf_scores: 모델에 user_id 없음 → fallback (user=%s)", user_id)
        return await _chronological_fallback(db, top_k)

    u_idx = user_ids.index(user_id)
    user_vec = user_factors[u_idx]  # (n_factors,)

    # MF 점수: user_factor @ item_factors.T → (n_posts,)
    mf_scores = item_factors @ user_vec  # (n_posts,)

    # pgvector cosine 보정: user_embedding <=> post_embeddings
    cosine_scores = np.zeros(len(post_ids))
    try:
        # 사용자 임베딩 조회
        ue_result = await db.execute(
            text("SELECT embedding::text FROM user_embeddings WHERE user_id = :uid"),
            {"uid": user_id},
        )
        ue_row = ue_result.fetchone()
        if ue_row and ue_row.embedding:
            # 벡터 파싱 및 정규화
            user_emb_vec = np.array(
                [float(x) for x in ue_row.embedding.strip("[]").split(",")]
            )

            # 포스트 임베딩 배치 조회 (top-k candidate pool만)
            candidate_post_ids = post_ids  # 전체 대상
            if len(candidate_post_ids) > 200:
                # 상위 200개 MF 후보만 cosine 계산 (성능)
                top200_idx = np.argsort(mf_scores)[-200:][::-1]
                candidate_post_ids = [post_ids[i] for i in top200_idx]

            placeholders = ", ".join(f"'{pid}'" for pid in candidate_post_ids)
            pe_result = await db.execute(
                text(f"""
                    SELECT post_id::text,
                           1 - (embedding <=> '{user_emb_vec.tolist()}'::vector) AS similarity
                    FROM post_embeddings
                    WHERE post_id IN ({placeholders})
                """)
            )
            for pe_row in pe_result.fetchall():
                if pe_row.post_id in post_ids:
                    p_idx = post_ids.index(pe_row.post_id)
                    cosine_scores[p_idx] = float(pe_row.similarity or 0)
    except Exception as exc:  # noqa: BLE001
        log.warning("_compute_mf_scores: pgvector 보정 실패 (%s) — MF만 사용", exc)

    # 최종 스코어: 0.7 * MF + 0.3 * cosine
    final_scores = 0.7 * mf_scores + 0.3 * cosine_scores

    # top_k 정렬
    top_indices = np.argsort(final_scores)[-top_k:][::-1]
    result_post_ids = [post_ids[i] for i in top_indices if i < len(post_ids)]

    return result_post_ids


async def _chronological_fallback(db, top_k: int) -> list[str]:
    """최신순 포스트 ID 리스트 반환 (fallback)."""
    from sqlalchemy import text

    result = await db.execute(
        text("""
            SELECT id::text FROM posts
            WHERE status = 'published'
              AND visibility = 'public'
            ORDER BY created_at DESC
            LIMIT :lim
        """),
        {"lim": top_k},
    )
    return [str(r.id) for r in result.fetchall()]


# ──────────────────────────────────────────────────────────────────────────────
# Cache invalidation
# ──────────────────────────────────────────────────────────────────────────────

async def invalidate_user_feed_cache(user_id: str) -> None:
    """사용자 피드 캐시 무효화 (interaction 발생 시 호출)."""
    await cache.delete(_cache_key(user_id), reason="interaction_update")
```

---

## 5. API Endpoints

### 5-1. 기존 `GET /api/feed` 수정

파일: `v1/backend/app/api/posts.py` (기존 feed 엔드포인트 수정)

**query parameter 추가**: `algo: str = Query("auto", regex="^(v1|v2|auto)$")`

```python
# posts.py — feed endpoint 수정 (핵심 변경 부분)

@router.get("/feed", response_model=list[PostOut])
async def get_feed(
    algo: str = Query("auto", description="v1=기존, v2=ML, auto=PostHog flag"),
    limit: int = Query(20, ge=1, le=50),
    db: AsyncSession = Depends(get_db),
    current_user: User | None = Depends(get_current_user_optional),
):
    """피드 엔드포인트 — algo 파라미터로 v1/v2/auto 분기.

    auto: PostHog feature flag 'ml_feed_v2' 기준으로 자동 결정.
    v2: ML 추천 (cold user → chronological fallback 자동).
    v1: 기존 feed_scoring 로직 (chronological / rule-based).
    """
    use_ml = _resolve_algo(algo, current_user)

    if use_ml and current_user:
        # ML v2 피드
        from app.services.ml_feed_inference import get_recommendations

        post_ids = await get_recommendations(db, str(current_user.id), top_k=limit)
        if post_ids:
            posts = await _load_posts_by_ids(db, post_ids)
            await _attach_active_auction_end_at(db, posts)
            return [_serialize_post(p) for p in posts]
        # post_ids 비어있으면 fallback (하단 기존 로직으로)

    # v1 / fallback: 기존 feed_scoring 또는 chronological
    return await _get_feed_v1(db, current_user, limit)


def _resolve_algo(algo: str, current_user) -> bool:
    """algo 파라미터 → ML 사용 여부 결정."""
    if algo == "v2":
        return True
    if algo == "v1":
        return False
    # auto: current_user 있고 PostHog flag ON 시 v2
    # PostHog server-side flag 조회 (간단 구현 — K-8에서 고도화)
    if current_user is None:
        return False
    flag_enabled = os.getenv("ML_FEED_V2_ENABLED", "false").lower() == "true"
    return flag_enabled
```

**응답 형식**: 기존 `PostOut[]` 동일 유지 — 프론트엔드 변경 불필요.

### 5-2. `POST /api/feed/interaction` (선택 — implicit feedback 수집)

```
POST /api/feed/interaction
Authorization: Bearer {accessToken}
Content-Type: application/json

Body:
{
  "post_id": "uuid",
  "interaction_type": "view" | "like" | "comment" | "sponsor" | "click",
  "weight": 1.0  (선택, 기본값 사용)
}

Response: 201 Created
```

- `view` 기록 시: 프론트엔드에서 ≥3초 체류 시 호출 (beacon API 활용)
- `like`, `comment`, `sponsor`: 기존 이벤트 핸들러에서 side-effect로 기록

---

## 6. Mock 모드 Fallback

### 환경변수 매트릭스

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `ML_TRAINING_WORKER_ENABLED` | `true` | `false` 시 cron worker 미등록 |
| `ML_TRAINING_DAYS` | `90` | 학습 데이터 기간 (일) |
| `ML_MF_FACTORS` | `50` | MF latent factor 수 |
| `ML_MF_ITERATIONS` | `20` | ALS 반복 횟수 |
| `ML_MIN_INTERACTIONS` | `100` | 최소 학습 데이터 수 |
| `ML_FEED_CACHE_TTL_SECONDS` | `300` | Redis 캐시 TTL (초) |
| `ML_COLD_USER_THRESHOLD` | `5` | Cold User 판별 기준 interaction 수 |
| `ML_FEED_TOP_K` | `20` | 추천 포스트 수 |
| `ML_FEED_V2_ENABLED` | `false` | auto 모드 시 v2 ON/OFF |
| `ML_TRAINING_INTERVAL_SECONDS` | `86400` | 학습 주기 (초, 24h) |

### Fallback 의사결정 트리

```
GET /api/feed?algo=auto|v2
│
├─ current_user 없음 → v1 (비로그인)
│
├─ ML_FEED_V2_ENABLED=false (algo=auto) → v1
│
├─ algo=v2 또는 flag ON
│   │
│   ├─ Redis 캐시 HIT → 즉시 반환 ✅
│   │
│   └─ Redis 캐시 MISS
│       │
│       ├─ interaction_count < 5 → Cold User → chronological fallback
│       │
│       ├─ ml_models active 없음 → Mock fallback → chronological
│       │
│       ├─ implicit/scipy/numpy 미설치 → WARNING + chronological fallback
│       │
│       └─ MF 점수 계산 성공 → Redis SET → 반환 ✅
│
└─ algo=v1 → 기존 feed_scoring (회귀 없음)
```

### CI/CD 권장 설정

```yaml
# GitHub Actions / docker-compose.test.yml
env:
  ML_TRAINING_WORKER_ENABLED: "false"   # CI에서 cron 미시작
  ML_FEED_V2_ENABLED: "false"           # CI에서 v2 미활성화 (auto 모드)
```

---

## 7. Test Plan

### 7-1. Unit Tests — `tests/unit/test_ml_feed_inference.py`

```python
"""Unit tests — ml_feed_inference.py (Phase 9 K-1)."""
import pytest
from unittest.mock import AsyncMock, MagicMock, patch

from app.services import ml_feed_inference


# ── Cache hit/miss ────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_get_recommendations_cache_hit():
    """Redis cache hit → DB 조회 없이 즉시 반환."""
    db = AsyncMock()
    expected = ["post-1", "post-2", "post-3"]

    with patch.object(ml_feed_inference.cache, "get_json", return_value=expected):
        result = await ml_feed_inference.get_recommendations(db, "user-1", top_k=3)

    assert result == expected
    db.execute.assert_not_called()


@pytest.mark.asyncio
async def test_get_recommendations_cache_miss_then_sets_cache():
    """cache miss → 계산 후 Redis SET 호출 확인."""
    db = AsyncMock()
    db.execute = AsyncMock(return_value=MagicMock(
        fetchone=lambda: None,
        scalar_one=lambda: 0,
        fetchall=lambda: [],
    ))

    with (
        patch.object(ml_feed_inference.cache, "get_json", return_value=None),
        patch.object(ml_feed_inference.cache, "set_json", new_callable=AsyncMock) as mock_set,
        patch.object(ml_feed_inference, "_get_interaction_count", return_value=0),
    ):
        await ml_feed_inference.get_recommendations(db, "user-new", top_k=5)

    # cold user fallback 시에도 캐시 미저장 (의도적 — fallback은 개인화 없음)


# ── Cold user fallback ────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_cold_user_fallback():
    """interaction < 5건 사용자 → chronological fallback 호출."""
    db = AsyncMock()
    chronological_posts = ["post-a", "post-b"]

    with (
        patch.object(ml_feed_inference.cache, "get_json", return_value=None),
        patch.object(ml_feed_inference, "_get_interaction_count", return_value=3),
        patch.object(
            ml_feed_inference, "_chronological_fallback",
            return_value=chronological_posts,
        ) as mock_fallback,
    ):
        result = await ml_feed_inference.get_recommendations(db, "cold-user", top_k=5)

    mock_fallback.assert_called_once()
    assert result == chronological_posts


# ── Mock 모드 (모델 미준비) ───────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_mock_mode_no_active_model():
    """ml_models active 없음 → fallback."""
    db = AsyncMock()
    fallback_posts = ["post-x", "post-y"]

    with (
        patch.object(ml_feed_inference.cache, "get_json", return_value=None),
        patch.object(ml_feed_inference, "_get_interaction_count", return_value=10),
        patch.object(ml_feed_inference, "_load_active_model", return_value=None),
        patch.object(
            ml_feed_inference, "_chronological_fallback",
            return_value=fallback_posts,
        ),
    ):
        result = await ml_feed_inference.get_recommendations(db, "user-1")

    assert result == fallback_posts


@pytest.mark.asyncio
async def test_chronological_fallback_structure():
    """_chronological_fallback: 반환 형식 확인."""
    db = AsyncMock()
    mock_rows = [MagicMock(id="post-1"), MagicMock(id="post-2")]
    db.execute = AsyncMock(return_value=MagicMock(fetchall=lambda: mock_rows))

    result = await ml_feed_inference._chronological_fallback(db, top_k=2)
    assert len(result) == 2
    assert all(isinstance(pid, str) for pid in result)
```

### 7-2. Unit Tests — `tests/unit/test_ml_feed_training.py`

```python
"""Unit tests — ml_feed_training.py (Phase 9 K-1)."""
import pytest
from unittest.mock import AsyncMock, MagicMock, patch

from app.services import ml_feed_training


# ── MF 학습 — 작은 numpy matrix ──────────────────────────────────────────────

def test_train_mf_model_numpy_mock():
    """numpy random mock 학습 → user_factors, item_factors 반환."""
    interactions = {
        "user_ids": ["u1", "u2", "u3"],
        "post_ids": ["p1", "p2", "p3", "p4"],
        "user_idx": {"u1": 0, "u2": 1, "u3": 2},
        "post_idx": {"p1": 0, "p2": 1, "p3": 2, "p4": 3},
        "interactions": [
            (0, 0, 3.0), (0, 1, 1.0),
            (1, 1, 2.0), (1, 2, 5.0),
            (2, 0, 1.0), (2, 3, 4.0),
            # MIN_INTERACTIONS 충족 위해 추가
        ] * 20,  # 120건 (> _MIN_INTERACTIONS=100)
    }

    # implicit 미설치, scipy 미설치, numpy만 있는 환경 시뮬레이션
    with (
        patch.dict("sys.modules", {"implicit": None, "scipy": None}),
    ):
        result = ml_feed_training.train_mf_model(interactions, n_factors=5, n_iterations=2)

    # numpy mock → None이 아닌 결과 반환
    # (실제 환경에서 numpy는 설치되어 있으므로 mock fallback 동작)
    # 결과 검증: 구조만 확인
    if result is not None:
        assert "user_factors" in result
        assert "item_factors" in result


def test_train_mf_model_empty_interactions():
    """빈 interactions → None 반환, 오류 없음."""
    result = ml_feed_training.train_mf_model({}, n_factors=5)
    assert result is None


def test_train_mf_model_insufficient_data():
    """interactions < MIN_INTERACTIONS → None 반환."""
    interactions = {
        "user_ids": ["u1"],
        "post_ids": ["p1"],
        "user_idx": {"u1": 0},
        "post_idx": {"p1": 0},
        "interactions": [(0, 0, 1.0)],  # 1건 < 100건
    }
    result = ml_feed_training.train_mf_model(interactions)
    assert result is None


# ── collect_interactions ──────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_collect_interactions_empty_db():
    """DB에 데이터 없음 → 빈 dict 반환."""
    db = AsyncMock()
    db.execute = AsyncMock(return_value=MagicMock(fetchall=lambda: []))

    result = await ml_feed_training.collect_interactions(db, days=90)
    assert result == {}


@pytest.mark.asyncio
async def test_collect_interactions_structure():
    """정상 데이터 → 올바른 구조 반환."""
    db = AsyncMock()
    mock_rows = [
        MagicMock(user_id="user-1", post_id="post-a", total_weight=3.0),
        MagicMock(user_id="user-1", post_id="post-b", total_weight=1.0),
        MagicMock(user_id="user-2", post_id="post-a", total_weight=2.0),
    ]
    db.execute = AsyncMock(return_value=MagicMock(fetchall=lambda: mock_rows))

    result = await ml_feed_training.collect_interactions(db, days=90)

    assert "user_ids" in result
    assert "post_ids" in result
    assert "interactions" in result
    assert len(result["user_ids"]) == 2  # user-1, user-2
    assert len(result["post_ids"]) == 2  # post-a, post-b
    assert len(result["interactions"]) == 3


# ── save_model_artifacts ──────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_save_model_artifacts_success():
    """모델 저장 → 모델 ID 반환."""
    db = AsyncMock()
    db.execute = AsyncMock(return_value=MagicMock(
        scalar_one=lambda: "model-uuid-1"
    ))
    db.commit = AsyncMock()

    model_result = {
        "user_factors": [[0.1, 0.2], [0.3, 0.4]],
        "item_factors": [[0.5, 0.6], [0.7, 0.8]],
    }
    interactions = {"user_ids": ["u1", "u2"], "post_ids": ["p1", "p2"]}

    result = await ml_feed_training.save_model_artifacts(
        db, model_result, interactions, version="mf-20260505"
    )

    assert result == "model-uuid-1"
    db.commit.assert_called_once()
```

### 7-3. Integration Tests — `tests/integration/test_feed_api_v2.py`

```python
"""Integration tests — /api/feed v2 ML 피드 엔드포인트 (Phase 9 K-1)."""
import pytest
from httpx import AsyncClient


@pytest.mark.asyncio
async def test_feed_algo_v1_returns_posts(client: AsyncClient, auth_headers: dict):
    """algo=v1 → 기존 피드 응답 형식 동일."""
    resp = await client.get("/api/feed?algo=v1&limit=5", headers=auth_headers)
    assert resp.status_code == 200
    data = resp.json()
    assert isinstance(data, list)


@pytest.mark.asyncio
async def test_feed_algo_v2_returns_posts(client: AsyncClient, auth_headers: dict):
    """algo=v2 → ML 피드 응답 (cold user fallback 포함) — 형식 동일."""
    resp = await client.get("/api/feed?algo=v2&limit=5", headers=auth_headers)
    assert resp.status_code == 200
    data = resp.json()
    assert isinstance(data, list)
    # 응답 형식: PostOut 구조 확인
    if data:
        assert "id" in data[0]
        assert "author" in data[0]
        assert "created_at" in data[0]


@pytest.mark.asyncio
async def test_feed_algo_auto_default(client: AsyncClient, auth_headers: dict):
    """algo=auto (기본) → ML_FEED_V2_ENABLED=false 시 v1과 동일 동작."""
    resp = await client.get("/api/feed?limit=5", headers=auth_headers)
    assert resp.status_code == 200


@pytest.mark.asyncio
async def test_feed_algo_v2_cold_user_fallback(
    client: AsyncClient,
    cold_user_auth_headers: dict,  # fixture: interaction 0건 사용자
):
    """interaction 0건 cold user → fallback 피드 반환 (200 OK)."""
    resp = await client.get("/api/feed?algo=v2&limit=5", headers=cold_user_auth_headers)
    assert resp.status_code == 200
    assert isinstance(resp.json(), list)


@pytest.mark.asyncio
async def test_feed_algo_invalid(client: AsyncClient, auth_headers: dict):
    """잘못된 algo 값 → 422 Validation Error."""
    resp = await client.get("/api/feed?algo=invalid", headers=auth_headers)
    assert resp.status_code == 422


@pytest.mark.asyncio
async def test_feed_interaction_post(client: AsyncClient, auth_headers: dict):
    """POST /api/feed/interaction → 201 Created."""
    resp = await client.post(
        "/api/feed/interaction",
        json={"post_id": "some-post-uuid", "interaction_type": "view"},
        headers=auth_headers,
    )
    # post_id 미존재 시 404 또는 201 (구현에 따라)
    assert resp.status_code in (201, 404)
```

---

## 8. Cron Worker 등록

### `app/main.py` 수정 (20번째 worker 추가)

```python
# main.py — imports 추가
from app.services.ml_feed_training import ml_training_cron_loop

# lifespan 내 task 등록 (ML_TRAINING_WORKER_ENABLED guard)
import os as _os
if _os.getenv("ML_TRAINING_WORKER_ENABLED", "true").lower() != "false":
    # 20th cron worker — ML feed training (R-5 isolated, daily 03:00 UTC, K-1)
    ml_training_task = asyncio.create_task(ml_training_cron_loop())
else:
    ml_training_task = None
    _log.info("ML training worker disabled (ML_TRAINING_WORKER_ENABLED=false)")

# finally 블록 cleanup 추가
if ml_training_task:
    ml_training_task.cancel()
    try:
        await ml_training_task
    except asyncio.CancelledError:
        pass
```

### Cron Worker 목록 (K-1 추가 후)

| # | Worker | 파일 | 주기 |
|---|--------|------|------|
| 1~11 | Phase 8 기존 workers | 각 jobs.py | 다양 |
| 12 | embedding_cron_loop | embedding_jobs.py | 60s + 86400s (L-A) |
| 13~19 | L-B ~ L-F workers | 각 jobs.py | 다양 |
| **20** | **ml_training_cron_loop** | **ml_feed_training.py** | **86400s (24h)** |

---

## 9. Frontend Changes

### 피드 페이지 — 변경 없음

ML 정렬은 서버측에서 처리되므로 프론트엔드 피드 페이지는 변경 불필요.
응답 형식 (`PostOut[]`) 동일 유지.

### implicit feedback 수집 (선택적 추가)

조회 시간 tracking을 위한 beacon 호출 (K-8 통합 시 PostHog와 함께 구현):

```typescript
// lib/hooks/useFeedInteraction.ts (선택 구현)
export function useFeedInteraction(postId: string) {
  const enteredAt = useRef<number>(Date.now());

  useEffect(() => {
    return () => {
      const elapsed = Date.now() - enteredAt.current;
      if (elapsed >= 3000) {
        // 3초 이상 조회 → view interaction 기록
        navigator.sendBeacon(
          "/api/feed/interaction",
          JSON.stringify({ post_id: postId, interaction_type: "view" })
        );
      }
    };
  }, [postId]);
}
```

### (선택) 사용자 설정 페이지 — ML 추천 토글

K-8 A/B 테스트 통합 시 추가 예정. 현재는 `algo` query param으로 개발자 테스트 가능.

---

## 10. 위임 Agent

| Agent | 담당 작업 | 산출물 |
|-------|----------|--------|
| `bkend-expert` | alembic 0073 + ml_feed_training.py + ml_feed_inference.py + main.py cron 등록 + unit/integration test | `0073_ml_feed_v2.py`, `ml_feed_training.py`, `ml_feed_inference.py`, `test_ml_feed_*.py` |
| `frontend-architect` | 피드 UI 변경 없음. 단, PostHog feature flag 통합(K-8)과 beacon interaction 수집 시 참여 | `useFeedInteraction.ts` (선택) |

### 병렬 실행 가능 범위

```
[병렬]
  bkend-expert: alembic 0073 작성 + ml_feed_training.py 작성
  (L-A embedding_jobs.py와 독립)

[순차]
  bkend-expert: ml_feed_inference.py (ml_feed_training.py 완성 후 모델 구조 확정)
  bkend-expert: posts.py feed endpoint 수정 (ml_feed_inference.py 완성 후)
  bkend-expert: main.py cron 등록
  bkend-expert: unit tests 작성 + 통과
  bkend-expert: integration tests 작성 + 통과

[검증]
  pytest tests/unit/test_ml_feed_*.py green
  pytest tests/integration/test_feed_api_v2.py green
  alembic upgrade head green
  GET /api/feed?algo=v2 → 200 OK (cold user fallback 포함)
  ML_TRAINING_WORKER_ENABLED=false → cron 미등록 확인
```

---

## 11. 작업 단계 (K-1 내부 순서)

| Step | 작업 | 담당 | 검증 |
|------|------|------|------|
| 1 | `alembic/versions/0073_ml_feed_v2.py` 작성 | bkend-expert | `alembic upgrade head` green |
| 2 | `app/services/ml_feed_training.py` 작성 | bkend-expert | `collect_interactions()`, `train_mf_model()` 시그니처 확인 |
| 3 | `app/services/ml_feed_inference.py` 작성 | bkend-expert | Mock 모드 fallback 동작 확인 |
| 4 | `app/api/posts.py` feed endpoint 수정 | bkend-expert | `GET /api/feed?algo=v1` 회귀 없음 |
| 5 | `app/main.py` cron worker 등록 (20번째) | bkend-expert | `ML_TRAINING_WORKER_ENABLED=false` guard 포함 |
| 6 | `tests/unit/test_ml_feed_training.py` 작성 + 통과 | bkend-expert | pytest green |
| 7 | `tests/unit/test_ml_feed_inference.py` 작성 + 통과 | bkend-expert | pytest green |
| 8 | `tests/integration/test_feed_api_v2.py` 작성 + 통과 | bkend-expert | pytest green |
| 9 | `GET /api/feed?algo=v2` cold user + mock 모드 E2E 확인 | bkend-expert | 200 OK + 올바른 형식 |
| 10 | tsc 0 errors + alembic head + 전체 test green | — | CI 통과 기준 |

---

## 12. 위험 & 완화

| 위험 | 가능성 | 완화 방안 |
|------|--------|----------|
| 50K events 부족으로 Precision@10 < 0.10 | 중 | 인기도 가중치 blend 추가 (K-2 diversity reranking booster), offline eval 결과에 따라 MF factors 축소 |
| MF 학습 시간 초과 (86400s 주기 내) | 낮 | batch_size 분할 학습 + 타임아웃 300s 설정, 초과 시 이전 모델 유지 |
| pgvector cosine 보정 쿼리 N+1 | 중 | candidate pool 200개 제한 + IN 절 배치 쿼리 (이미 설계에 반영) |
| Redis 미연결 시 캐시 없이 DB 직접 조회 | 낮 | CacheClient Mock 모드 자동 활성화 (기존 cache.py 설계), latency 허용 범위 확인 |
| MF model params JSONB 크기 | 중 | user_factors (1000×50) + item_factors (5000×50) ≈ 3.6MB JSON. JSONB 압축 + 추후 S3 외부화 고려 |
| implicit 라이브러리 라이선스 | 낮 | MIT 라이선스, 상업적 사용 가능. requirements.txt에 `implicit>=0.7.0` 추가 |
| A/B 분기 비율 부족 (통계적 유의성) | 중 | 최소 7일 운영 + 500 unique users 필요. K-8에서 PostHog 샘플 크기 계산 포함 |
| model params 기반 추론 — 사용자/포스트 ID 순서 의존 | 중 | params에 user_ids, post_ids 리스트 함께 저장 (설계에 반영), 모델 재학습 시 전체 갱신 |

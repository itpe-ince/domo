---
template: design
version: 1.0
feature: ai-featured-artist
phase: 10 / K-4
date: 2026-05-05
author: itpe-ince (Claude Sonnet 4.6)
project: domo
project_version: v1
parent_plan: domo-phase10-roadmap.plan.md
status: Draft
---

# Phase 10 K-4 Design — AI Featured Artist 주간 자동 선정

> **Summary**: Phase 7 G'-7(admin 수동 featured) 패턴을 ML 자동화로 전환한다.
> L-A `user_embeddings`, Phase 6 `artist_index_rank`, Phase 7 G'-9 `post_engagement_cache`를
> 조합해 주 1회 자동으로 Featured Artist 후보를 선정하고, admin 검수 큐를 통해 publish한다.
> autopublish OFF(admin 검수 필수), 주 5명 후보, 다양성 보정 포함.

---

## 1. 목표 & Acceptance Criteria

### 목표

| # | 목표 | 근거 |
|---|------|------|
| 1 | Featured Artist 후보 선정 자동화 | admin 큐레이터 수동 탐색 부담 제거 → 주당 검토 시간 ↓ |
| 2 | 신진작가 + 다양성 우선 선정 | 그로스해킹 funnel 좌측(후원자 0명) 작가 우선 노출 → 후원 전환 |
| 3 | admin 검수 큐 도입 | ML 스코어 어뷰징 방지 + 최종 품질 보장 (autopublish OFF) |
| 4 | Phase 8 G'-7 `featured_artists` 테이블 통합 | publish 시 기존 테이블 INSERT → UI 변경 최소화 |
| 5 | Fallback graceful 처리 | 의존 데이터(임베딩·artist_index·engagement) 부족 시 안전 동작 |

### Acceptance Criteria (K-4 종결 조건)

- [ ] alembic 0082 적용 후 `featured_artist_candidates` 테이블 생성 확인 (`alembic upgrade head` green)
- [ ] `UNIQUE INDEX (artist_id, week_start)` — 같은 작가·같은 주 중복 INSERT 방지 동작 확인
- [ ] `featured_artist_jobs.py` 주 1회 cron 동작 (월요일 09:00 UTC, 22번째 worker)
- [ ] `select_candidates_for_week()` — composite_score 4가지 가중치 계산 및 JSONB reasoning 기록 확인
- [ ] 후보 5명 INSERT → `featured_artist_candidates.status = 'pending'`
- [ ] 후보 < 3명 시 graceful: admin Slack 알림 + WARNING 로그 (예외 미발생)
- [ ] `GET /api/admin/featured-artist/candidates` — 현재 주 pending 목록 반환 (admin 권한 검증)
- [ ] `POST /api/admin/featured-artist/candidates/{id}/approve` → `status='approved'` 전환
- [ ] `POST /api/admin/featured-artist/candidates/{id}/publish` → `featured_artists` 테이블 INSERT + `status='published'`
- [ ] `POST /api/admin/featured-artist/candidates/{id}/reject` → `status='rejected'` + 사유 저장
- [ ] `FEATURED_ARTIST_WORKER_ENABLED=false` 시 cron worker 등록 건너뜀
- [ ] unit tests `tests/unit/test_featured_artist_jobs.py` 6+ 케이스 통과
- [ ] integration tests `tests/integration/test_featured_artist_admin.py` 4+ 케이스 통과

### KPIs

| 지표 | 목표 | 측정 도구 |
|------|:----:|----------|
| admin 수락률 (자동 선정 정확도) | ≥ 70% | DB 쿼리: `status='approved'` / `status IN ('approved','rejected')` |
| Featured Artist CTR (홈/피드) | ≥ 15% | PostHog `featured_artist_click` 이벤트 |
| 신진작가 선정 비율 | ≥ 70% | DB: `artist_index_rank > 80th percentile` |
| 후원 전환율 (Featured → 후원) | baseline 측정 시작 | PostHog `sponsor_created` funnel |
| admin 검수 시간 | 수동 대비 ↓ | 정성 측정 (admin 피드백) |

---

## 2. Architecture

### 데이터 플로우

```
[Phase 6] artist_index_rank (hourly cron)
    ↓ artist_index_score, follower_count, region, genre
[Phase 7 G'-9] post_engagement_cache (hourly cron)
    ↓ engagement_score (최근 14일)
[Phase 9 L-A] user_embeddings, post_embeddings (daily batch)
    ↓ 128차원 벡터 (장르·지역 다양성 분산 계산용)
         ↓
[K-4 Cron] featured_artist_jobs.select_candidates_for_week()
    ├── 기준 1: artist_index_rank > 80th percentile (신진작가)
    ├── 기준 2: post_engagement_cache 최근 14일 상위 30%
    ├── 기준 3: user_embeddings 기반 다양성 (장르·지역 분산)
    ├── 기준 4: 후원자 0건 우선 (funnel 좌측)
    └── composite_score 합산 → 상위 5명
         ↓ INSERT
[featured_artist_candidates] status='pending'
         ↓ admin 검수
    ├── approve: status='approved'
    ├── reject:  status='rejected' + 사유
    └── publish: status='published'
              ↓ INSERT
         [featured_artists] (G'-7 기존 테이블)
              ↓
         [홈/피드 UI 노출]
```

### 의존 컴포넌트

| 컴포넌트 | Phase | 역할 | Fallback |
|---------|:-----:|------|---------|
| `artist_index` (users 컬럼) | 6 | 신진작가 랭킹, follower_count | 단순 follower 정렬 |
| `post_engagement_cache` | 7 G'-9 | 최근 engagement_score | 정적 like_count 기반 |
| `user_embeddings` | 9 L-A | 다양성 분산 계산 | 다양성 보정 skip |
| `featured_artists` | 8 G'-7 | publish 대상 테이블 | — |
| Slack 알림 | 9 L-F | 후보 < 3 알림 패턴 재사용 | log WARNING만 |

---

## 3. Database Schema (alembic 0082)

### 파일: `v1/backend/alembic/versions/0082_featured_artist_candidates.py`

```python
"""alembic 0082 — featured_artist_candidates (K-4 AI Featured Artist 자동 선정)

Phase 10 K-4: admin 검수 큐 도입. autopublish OFF 정책.
publish 시 Phase 8 G'-7 featured_artists 테이블에 INSERT.

Depends: 0081_diversity_constraints (K-2)
"""
from __future__ import annotations

from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = "0082"
down_revision = "0081_diversity_config"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # ENUM 타입 생성
    op.execute("""
        CREATE TYPE featured_candidate_status AS ENUM
        ('pending', 'approved', 'rejected', 'published', 'expired')
    """)

    op.execute("""
        CREATE TABLE featured_artist_candidates (
            id              UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
            artist_id       UUID        NOT NULL
                                REFERENCES users(id) ON DELETE CASCADE,
            week_start      DATE        NOT NULL,
            composite_score FLOAT       NOT NULL,
            reasoning       JSONB       NOT NULL DEFAULT '{}',
            status          featured_candidate_status NOT NULL DEFAULT 'pending',
            admin_id        UUID        REFERENCES users(id) ON DELETE SET NULL,
            reviewed_at     TIMESTAMPTZ,
            published_at    TIMESTAMPTZ,
            created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
        )
    """)

    # 같은 작가 같은 주 중복 방지
    op.create_index(
        "uq_featured_artist_candidates_artist_week",
        "featured_artist_candidates",
        ["artist_id", "week_start"],
        unique=True,
    )

    # admin 검수 큐 조회 최적화
    op.create_index(
        "ix_featured_artist_candidates_status_week",
        "featured_artist_candidates",
        ["status", "week_start"],
    )

    # 특정 주 전체 후보 조회
    op.create_index(
        "ix_featured_artist_candidates_week_start",
        "featured_artist_candidates",
        ["week_start"],
    )


def downgrade() -> None:
    op.drop_index("ix_featured_artist_candidates_week_start",
                  table_name="featured_artist_candidates")
    op.drop_index("ix_featured_artist_candidates_status_week",
                  table_name="featured_artist_candidates")
    op.drop_index("uq_featured_artist_candidates_artist_week",
                  table_name="featured_artist_candidates")
    op.execute("DROP TABLE IF EXISTS featured_artist_candidates")
    op.execute("DROP TYPE IF EXISTS featured_candidate_status")
```

### 스키마 요약

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID PK | auto gen_random_uuid() |
| `artist_id` | UUID FK users | 후보 작가 |
| `week_start` | DATE | 해당 주 월요일 (예: 2026-05-04) |
| `composite_score` | FLOAT | 선정 점수 (0.0~1.0 정규화) |
| `reasoning` | JSONB | 선정 사유 분해: `{engagement, rank, diversity, new_artist_bonus}` |
| `status` | ENUM | `pending` → `approved` / `rejected` → `published` / `expired` |
| `admin_id` | UUID FK users nullable | 검수한 admin |
| `reviewed_at` | TIMESTAMPTZ nullable | 검수 시각 |
| `published_at` | TIMESTAMPTZ nullable | publish 시각 |
| `created_at` | TIMESTAMPTZ | cron INSERT 시각 |

**설계 결정**

- `week_start = DATE`: 주별 관리를 위해 TIMESTAMPTZ 대신 DATE 사용. cron에서 `date_trunc('week', now())::date`로 계산.
- `reasoning JSONB`: 4가지 가중치 분해 값 저장 → admin이 선정 근거 확인 + 추후 알고리즘 튜닝 데이터 활용.
- `status ENUM`: `expired`는 해당 주가 지나도 publish 안 된 후보를 주기적으로 expire 처리할 때 사용.
- UNIQUE INDEX `(artist_id, week_start)`: cron 재실행 시 ON CONFLICT DO NOTHING으로 멱등성 보장.

---

## 4. Service Layer

### 파일: `app/services/featured_artist_jobs.py` (신규)

#### 4-1. 핵심 알고리즘: `select_candidates_for_week()`

**composite_score 계산식**

```
composite_score = w1 × engagement_score_norm
                + w2 × artist_rank_norm
                + w3 × diversity_score
                + w4 × new_artist_bonus

가중치 (OQ-4 권장, 튜닝 가능):
  w1 = 0.30  (post_engagement_cache 최근 14일)
  w2 = 0.30  (artist_index_rank 백분위)
  w3 = 0.20  (user_embeddings 다양성 분산)
  w4 = 0.20  (후원자 0건 신진작가 보너스)
```

**선정 기준 상세**

| 기준 | 조건 | 처리 |
|------|------|------|
| 기준 1 (신진작가) | `artist_index_rank > 80th percentile` 또는 `follower_count < 1000` | 대상 pool 필터링 |
| 기준 2 (engagement) | `post_engagement_cache` 최근 14일 상위 30% | 점수 정규화 (0~1) |
| 기준 3 (다양성) | `user_embeddings` 장르·지역 분산 (선정 후보 간 코사인 거리 최대화) | MMR 방식 보정 |
| 기준 4 (funnel) | 후원자 0건 작가 → bonus +0.20 | 그로스해킹 funnel 좌측 우선 |
| 중복 방지 | 최근 4주 내 이미 선정된 작가 제외 | `featured_artists` 테이블 쿼리 |

```python
"""featured_artist_jobs.py — K-4 AI Featured Artist 주간 자동 선정.

R-5 격리 패턴 준수:
  - 별도 파일 (featured_artist_jobs.py)
  - AsyncSessionLocal 독립 사용
  - 개별 Prometheus metric label

동작:
  매주 월요일 09:00 UTC: select_candidates_for_week() → top-5 INSERT
  후보 < 3명 시: admin Slack 알림 + WARNING 로그

FEATURED_ARTIST_WORKER_ENABLED=false 환경변수로 worker 비활성화 가능.
"""
from __future__ import annotations

import asyncio
import logging
import os
from datetime import date, datetime, timedelta, timezone
from typing import Any

from sqlalchemy import text

from app.db.session import AsyncSessionLocal

log = logging.getLogger(__name__)

# ─── 가중치 상수 (환경변수로 튜닝 가능) ──────────────────────────────────────
_W_ENGAGEMENT = float(os.getenv("FEATURED_W_ENGAGEMENT", "0.30"))
_W_RANK       = float(os.getenv("FEATURED_W_RANK", "0.30"))
_W_DIVERSITY  = float(os.getenv("FEATURED_W_DIVERSITY", "0.20"))
_W_NEW_ARTIST = float(os.getenv("FEATURED_W_NEW_ARTIST", "0.20"))
_TOP_N        = int(os.getenv("FEATURED_TOP_N", "5"))
_LOW_CANDIDATE_THRESHOLD = 3

SLACK_WEBHOOK_URL = os.getenv("SLACK_WEBHOOK_URL", "")


def _current_week_start() -> date:
    """이번 주 월요일 날짜 반환."""
    today = date.today()
    return today - timedelta(days=today.weekday())


async def _notify_admin_low_candidates(week_start: date, count: int) -> None:
    """후보 < 3명 시 Slack 알림 (L-F 패턴 재사용)."""
    if not SLACK_WEBHOOK_URL:
        log.warning(
            "featured_artist_jobs: 후보 %d명 (< %d) — SLACK_WEBHOOK_URL 미설정, 알림 skip",
            count, _LOW_CANDIDATE_THRESHOLD,
        )
        return
    try:
        import httpx
        msg = (
            f":warning: *Featured Artist 후보 부족*\n"
            f"주간: {week_start.isoformat()}, 후보 수: {count}명 (기준: {_LOW_CANDIDATE_THRESHOLD}명)\n"
            f"수동 모드로 전환 필요: <https://console.bkend.ai|Domo Admin>"
        )
        async with httpx.AsyncClient(timeout=10) as client:
            await client.post(SLACK_WEBHOOK_URL, json={"text": msg})
    except Exception as exc:  # noqa: BLE001
        log.warning("featured_artist_jobs: Slack 알림 실패: %s", exc)


async def select_candidates_for_week(
    db,
    week_start: date | None = None,
    n: int = _TOP_N,
) -> list[dict[str, Any]]:
    """주간 Featured Artist 후보 n명 선정.

    반환: [{"artist_id": str, "composite_score": float, "reasoning": dict}, ...]
    후보 < _LOW_CANDIDATE_THRESHOLD 시 Slack 알림 + WARNING 로그.
    """
    if week_start is None:
        week_start = _current_week_start()

    four_weeks_ago = week_start - timedelta(weeks=4)

    # ── Step 1: 기준 1+2 결합 쿼리 ──────────────────────────────────────────
    # artist_index_rank > 80th percentile OR follower_count < 1000
    # + post_engagement_cache 최근 14일 집계
    # + 최근 4주 선정 작가 제외
    # + 이미 이번 주 후보로 INSERT된 작가 제외 (중복 방지)
    engagement_window = datetime.now(timezone.utc) - timedelta(days=14)

    try:
        result = await db.execute(
            text("""
                WITH ranked_artists AS (
                    SELECT
                        u.id                    AS artist_id,
                        u.follower_count         AS follower_count,
                        COALESCE(u.artist_index_rank, 0)   AS artist_rank,
                        COALESCE(u.artist_index_score, 0)  AS artist_score,
                        u.region,
                        u.genre
                    FROM users u
                    WHERE u.role = 'artist'
                      AND (
                          u.artist_index_rank > (
                              SELECT PERCENTILE_CONT(0.8) WITHIN GROUP (ORDER BY artist_index_rank)
                              FROM users WHERE role = 'artist' AND artist_index_rank IS NOT NULL
                          )
                          OR u.follower_count < 1000
                      )
                      -- 최근 4주 featured 제외
                      AND u.id NOT IN (
                          SELECT user_id FROM featured_artists
                          WHERE created_at >= :four_weeks_ago
                      )
                      -- 이번 주 이미 후보 제외
                      AND u.id NOT IN (
                          SELECT artist_id FROM featured_artist_candidates
                          WHERE week_start = :week_start
                      )
                ),
                engagement_scores AS (
                    SELECT
                        p.author_id             AS artist_id,
                        SUM(pec.engagement_score) AS total_engagement
                    FROM post_engagement_cache pec
                    JOIN posts p ON p.id = pec.post_id
                    WHERE pec.updated_at >= :engagement_window
                    GROUP BY p.author_id
                ),
                sponsor_counts AS (
                    SELECT
                        artist_id,
                        COUNT(*) AS sponsor_count
                    FROM sponsorships
                    WHERE status = 'active'
                    GROUP BY artist_id
                )
                SELECT
                    ra.artist_id::text,
                    ra.artist_rank,
                    ra.follower_count,
                    ra.region,
                    ra.genre,
                    COALESCE(es.total_engagement, 0) AS engagement_score,
                    COALESCE(sc.sponsor_count, 0)     AS sponsor_count
                FROM ranked_artists ra
                LEFT JOIN engagement_scores es ON es.artist_id = ra.artist_id
                LEFT JOIN sponsor_counts sc ON sc.artist_id = ra.artist_id
                ORDER BY es.total_engagement DESC NULLS LAST
            """),
            {
                "four_weeks_ago": four_weeks_ago,
                "week_start": week_start,
                "engagement_window": engagement_window,
            },
        )
        rows = result.fetchall()
    except Exception as exc:  # noqa: BLE001
        log.warning("featured_artist_jobs: DB 쿼리 실패 (fallback): %s", exc)
        rows = []

    if not rows:
        log.warning("featured_artist_jobs: 후보 pool이 비어있음 (week_start=%s)", week_start)
        await _notify_admin_low_candidates(week_start, 0)
        return []

    # ── Step 2: 정규화 ────────────────────────────────────────────────────────
    max_engagement = max((r.engagement_score for r in rows), default=1.0) or 1.0
    max_rank = max((r.artist_rank for r in rows), default=1.0) or 1.0

    scored: list[dict[str, Any]] = []
    for row in rows:
        engagement_norm = row.engagement_score / max_engagement
        rank_norm = row.artist_rank / max_rank
        new_artist_bonus = _W_NEW_ARTIST if row.sponsor_count == 0 else 0.0

        # ── Step 3: 다양성 점수 (user_embeddings 미사용 시 0.0 fallback) ────
        diversity_score = 0.0  # 임베딩 기반 MMR은 후보군 선정 후 적용

        raw_score = (
            _W_ENGAGEMENT * engagement_norm
            + _W_RANK * rank_norm
            + _W_DIVERSITY * diversity_score
            + new_artist_bonus
        )
        scored.append({
            "artist_id": row.artist_id,
            "composite_score": raw_score,
            "reasoning": {
                "engagement": round(engagement_norm, 4),
                "rank": round(rank_norm, 4),
                "diversity": round(diversity_score, 4),
                "new_artist_bonus": round(new_artist_bonus, 4),
                "sponsor_count": int(row.sponsor_count),
                "follower_count": int(row.follower_count),
                "region": row.region,
                "genre": row.genre,
            },
        })

    # ── Step 4: diversity MMR — 장르·지역 분산 보정 ───────────────────────────
    scored = _apply_diversity_mmr(scored, n * 3)  # 후보 풀 3배에서 MMR 적용

    # ── Step 5: 상위 n명 반환 ─────────────────────────────────────────────────
    top_n = sorted(scored, key=lambda x: x["composite_score"], reverse=True)[:n]

    if len(top_n) < _LOW_CANDIDATE_THRESHOLD:
        log.warning(
            "featured_artist_jobs: 후보 %d명 (< %d, week_start=%s)",
            len(top_n), _LOW_CANDIDATE_THRESHOLD, week_start,
        )
        await _notify_admin_low_candidates(week_start, len(top_n))

    return top_n


def _apply_diversity_mmr(
    scored: list[dict[str, Any]],
    pool_size: int,
) -> list[dict[str, Any]]:
    """장르·지역 다양성 보정 (Maximal Marginal Relevance 단순화 버전).

    user_embeddings 미사용 버전: 장르·지역 태그 기반 분산.
    선택된 집합에 이미 있는 장르/지역의 중복 패널티를 composite_score에 반영.
    """
    pool = sorted(scored, key=lambda x: x["composite_score"], reverse=True)[:pool_size]

    selected: list[dict[str, Any]] = []
    seen_genres: dict[str, int] = {}
    seen_regions: dict[str, int] = {}

    for item in pool:
        genre = item["reasoning"].get("genre") or "unknown"
        region = item["reasoning"].get("region") or "unknown"

        genre_penalty = min(seen_genres.get(genre, 0) * 0.05, 0.15)
        region_penalty = min(seen_regions.get(region, 0) * 0.03, 0.09)

        adjusted = item["composite_score"] - genre_penalty - region_penalty
        item["composite_score"] = max(adjusted, 0.0)
        item["reasoning"]["diversity"] = round(genre_penalty + region_penalty, 4)

        seen_genres[genre] = seen_genres.get(genre, 0) + 1
        seen_regions[region] = seen_regions.get(region, 0) + 1
        selected.append(item)

    return selected


async def insert_candidates(
    db,
    candidates: list[dict[str, Any]],
    week_start: date,
) -> int:
    """후보 목록을 featured_artist_candidates에 INSERT (ON CONFLICT DO NOTHING, 멱등).

    반환: 실제 INSERT된 행 수.
    """
    inserted = 0
    for c in candidates:
        try:
            result = await db.execute(
                text("""
                    INSERT INTO featured_artist_candidates
                        (artist_id, week_start, composite_score, reasoning, status)
                    VALUES (:artist_id, :week_start, :score, :reasoning::jsonb, 'pending')
                    ON CONFLICT (artist_id, week_start) DO NOTHING
                    RETURNING id
                """),
                {
                    "artist_id": c["artist_id"],
                    "week_start": week_start,
                    "score": c["composite_score"],
                    "reasoning": __import__("json").dumps(c["reasoning"]),
                },
            )
            if result.fetchone():
                inserted += 1
        except Exception as exc:  # noqa: BLE001
            log.warning("insert_candidates: %s 실패: %s", c["artist_id"], exc)

    await db.commit()
    log.info("insert_candidates: %d/%d 삽입 완료 (week_start=%s)", inserted, len(candidates), week_start)
    return inserted


async def expire_old_candidates(db, week_start: date) -> None:
    """이번 주보다 이전 주의 pending 후보를 expired로 전환."""
    await db.execute(
        text("""
            UPDATE featured_artist_candidates
            SET status = 'expired'
            WHERE status = 'pending'
              AND week_start < :week_start
        """),
        {"week_start": week_start},
    )
    await db.commit()


# ─── Cron loop ───────────────────────────────────────────────────────────────

_FEATURED_INTERVAL_SECONDS = int(os.getenv("FEATURED_ARTIST_INTERVAL_SECONDS", "604800"))  # 7일


async def feature_artist_cron_loop() -> None:
    """주 1회 월요일 09:00 UTC 실행.

    즉시 첫 실행 후 7일 sleep — 서버 재시작 시 자동 복구.
    """
    import math

    while True:
        # 다음 월요일 09:00 UTC까지 대기
        now = datetime.now(timezone.utc)
        days_until_monday = (7 - now.weekday()) % 7
        if days_until_monday == 0 and now.hour < 9:
            next_run = now.replace(hour=9, minute=0, second=0, microsecond=0)
        elif days_until_monday == 0 and now.hour >= 9:
            next_run = now.replace(hour=9, minute=0, second=0, microsecond=0) + timedelta(weeks=1)
        else:
            next_run = (now + timedelta(days=days_until_monday)).replace(
                hour=9, minute=0, second=0, microsecond=0
            )

        wait_seconds = (next_run - now).total_seconds()
        log.info("featured_artist_cron_loop: 다음 실행 %s (%.0f초 후)", next_run.isoformat(), wait_seconds)

        if wait_seconds > 0:
            await asyncio.sleep(wait_seconds)

        async with AsyncSessionLocal() as db:
            try:
                week_start = _current_week_start()
                log.info("featured_artist_cron_loop: 실행 시작 (week_start=%s)", week_start)

                candidates = await select_candidates_for_week(db, week_start)
                if candidates:
                    await insert_candidates(db, candidates, week_start)
                await expire_old_candidates(db, week_start)

                log.info("featured_artist_cron_loop: 완료 (후보 %d명)", len(candidates))
            except Exception as exc:  # noqa: BLE001
                log.warning("featured_artist_cron_loop: 오류 (무시): %s", exc)
```

#### 4-2. Mock 모드 Fallback 매트릭스

| 의존 데이터 | 미가용 조건 | Fallback 동작 |
|------------|-----------|-------------|
| `artist_index_rank` | `NULL` | `follower_count < 1000` 기준으로 대체 |
| `post_engagement_cache` | 테이블 없음 / 빈 값 | `like_count + comment_count` 정적 집계 |
| `user_embeddings` | 미생성 | 다양성 보정 skip (diversity_score = 0.0) |
| 후보 < 3명 | 전체 pool 부족 | Slack 알림 + manual 모드 제안 + 빈 리스트 반환 |
| DB 쿼리 실패 | 예외 발생 | 빈 리스트 반환 + WARNING 로그 (cron loop 미중단) |

---

## 5. API Endpoints (admin 전용)

모든 엔드포인트: `Authorization: Bearer {adminToken}` + admin role 검증 필수.

### 5-1. `GET /api/admin/featured-artist/candidates`

현재 주 pending 후보 목록 반환.

**Request**

```
GET /api/admin/featured-artist/candidates
Headers:
  Authorization: Bearer {adminToken}
Query Params:
  week_start: YYYY-MM-DD (optional, default: 이번 주 월요일)
  status: pending|approved|rejected|published (optional, default: pending)
```

**Response 200**

```json
{
  "week_start": "2026-05-04",
  "candidates": [
    {
      "id": "uuid",
      "artist_id": "uuid",
      "artist_name": "이서연",
      "artist_avatar_url": "https://...",
      "follower_count": 87,
      "week_start": "2026-05-04",
      "composite_score": 0.76,
      "reasoning": {
        "engagement": 0.85,
        "rank": 0.72,
        "diversity": 0.03,
        "new_artist_bonus": 0.20,
        "sponsor_count": 0,
        "follower_count": 87,
        "region": "KR",
        "genre": "watercolor"
      },
      "status": "pending",
      "created_at": "2026-05-04T09:00:00Z"
    }
  ],
  "total": 5
}
```

**Response 403**: admin 권한 없음

### 5-2. `POST /api/admin/featured-artist/candidates/{id}/approve`

pending → approved 전환 (publish는 별도 액션).

**Request**

```
POST /api/admin/featured-artist/candidates/{id}/approve
Headers:
  Authorization: Bearer {adminToken}
Body: {} (빈 바디 허용)
```

**Response 200**

```json
{
  "id": "uuid",
  "status": "approved",
  "admin_id": "uuid",
  "reviewed_at": "2026-05-05T10:30:00Z"
}
```

**Response 409**: 이미 approved/rejected/published 상태

### 5-3. `POST /api/admin/featured-artist/candidates/{id}/publish`

approved → published 전환 + `featured_artists` 테이블 INSERT (Phase 8 G'-7 통합).

autopublish OFF 정책: approve 후 별도 publish 액션 필수.

**Request**

```
POST /api/admin/featured-artist/candidates/{id}/publish
Headers:
  Authorization: Bearer {adminToken}
Body: {
  "notes": "string (optional, 노출 문구 등 메모)"
}
```

**처리 로직**

1. `featured_artist_candidates.status` = `'approved'` 확인 (아닐 시 409)
2. `featured_artists` 테이블 INSERT (G'-7 스키마 준수):
   ```sql
   INSERT INTO featured_artists (user_id, featured_at, featured_by, notes)
   VALUES (:artist_id, now(), :admin_id, :notes)
   ```
3. `featured_artist_candidates.status` = `'published'`, `published_at` = now() 업데이트

**Response 200**

```json
{
  "id": "uuid",
  "status": "published",
  "featured_artist_id": "uuid",
  "published_at": "2026-05-05T10:35:00Z"
}
```

**Response 409**: status != 'approved' (approve 먼저 필요)

### 5-4. `POST /api/admin/featured-artist/candidates/{id}/reject`

pending 또는 approved → rejected 전환 + 사유 저장.

**Request**

```
POST /api/admin/featured-artist/candidates/{id}/reject
Headers:
  Authorization: Bearer {adminToken}
Body: {
  "reason": "string (required, 거부 사유)"
}
```

**처리 로직**

1. `featured_artist_candidates.status` = `'rejected'`
2. `reviewed_at` = now(), `admin_id` = 요청 admin
3. `reasoning` JSONB에 `"reject_reason"` 키 merge

**Response 200**

```json
{
  "id": "uuid",
  "status": "rejected",
  "reviewed_at": "2026-05-05T10:40:00Z"
}
```

---

## 6. Frontend Changes (admin 페이지)

K-4 backend 우선. frontend는 기본 admin 큐 페이지를 제공하고, 고도화는 별도 sub-PDCA로 분리 가능.

### 6-1. `/admin/featured-artist-queue` 페이지 (admin 전용)

**기능 요구사항**

- 현재 주 pending 후보 목록 테이블 (5행)
- 각 행: 작가 아바타, 이름, follower_count, composite_score, reasoning 분해 (engagement/rank/diversity/bonus)
- 액션 버튼: [승인] [거부] [게시]
- 게시 버튼: approve 상태에서만 활성화
- 거부 시 reason 입력 모달

**컴포넌트 구조**

```
/admin/featured-artist-queue/page.tsx
  └── FeaturedArtistCandidateTable
        ├── CandidateRow (후보 1행)
        │     ├── ArtistInfo (아바타, 이름, 팔로워)
        │     ├── ScoreBreakdown (4가지 가중치 시각화)
        │     └── ActionButtons (approve/publish/reject)
        └── RejectReasonModal
```

**i18n 키 (5 locale: ko/en/ja/zh/es)**

```json
{
  "admin.featured_artist.title": "주간 Featured Artist 후보",
  "admin.featured_artist.approve": "승인",
  "admin.featured_artist.publish": "게시",
  "admin.featured_artist.reject": "거부",
  "admin.featured_artist.reject_reason": "거부 사유",
  "admin.featured_artist.score_breakdown": "선정 근거",
  "admin.featured_artist.low_candidates": "후보가 부족합니다 ({{count}}명). 수동 선정을 검토하세요."
}
```

### 6-2. 선택사항: 홈/피드 UI 노출

Phase 8 G'-7에서 이미 `featured_artists` 테이블을 사용하는 UI가 있다면, K-4 publish 후 자동 노출됨 (UI 변경 최소화).

신규 "주간 자동 선정" 배지 추가 여부는 별도 검토.

---

## 7. Cron Worker 등록

### `app/main.py` — 22번째 cron worker 추가

```python
# main.py imports 추가
from app.services.featured_artist_jobs import feature_artist_cron_loop

# lifespan 내 task 등록 (FEATURED_ARTIST_WORKER_ENABLED guard)
import os as _os
if _os.getenv("FEATURED_ARTIST_WORKER_ENABLED", "true").lower() != "false":
    # 22nd cron worker — featured artist (R-5 isolated, weekly Monday 09:00 UTC, K-4)
    featured_artist_task = asyncio.create_task(feature_artist_cron_loop())
else:
    featured_artist_task = None
```

### 환경변수 매트릭스

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `FEATURED_ARTIST_WORKER_ENABLED` | `true` | `false` 시 cron 미등록 |
| `FEATURED_TOP_N` | `5` | 주당 후보 수 |
| `FEATURED_W_ENGAGEMENT` | `0.30` | engagement 가중치 |
| `FEATURED_W_RANK` | `0.30` | artist_rank 가중치 |
| `FEATURED_W_DIVERSITY` | `0.20` | 다양성 가중치 |
| `FEATURED_W_NEW_ARTIST` | `0.20` | 신진작가 보너스 가중치 |
| `SLACK_WEBHOOK_URL` | (없음) | 후보 부족 알림 (공유: L-F 패턴) |

---

## 8. Test Plan

### 8-1. Unit Tests: `tests/unit/test_featured_artist_jobs.py` (6+ 케이스)

```python
"""Unit tests — featured_artist_jobs.py (Phase 10 K-4)."""
import json
import pytest
from datetime import date
from unittest.mock import AsyncMock, MagicMock, patch

from app.services import featured_artist_jobs


# ── 1. composite_score 점수 계산 (mock data) ─────────────────────────────────

@pytest.mark.asyncio
async def test_select_candidates_score_calculation():
    """engagement + rank + bonus 가중합 점수 계산 검증."""
    db = AsyncMock()
    # engagement_score=1.0, artist_rank=100(max), sponsor_count=0 → max score
    mock_row = MagicMock(
        artist_id="artist-uuid-1",
        artist_rank=100.0,
        follower_count=50,
        region="KR",
        genre="watercolor",
        engagement_score=100.0,
        sponsor_count=0,
    )
    db.execute = AsyncMock(return_value=MagicMock(fetchall=lambda: [mock_row]))
    db.commit = AsyncMock()

    with patch("app.services.featured_artist_jobs._notify_admin_low_candidates", new=AsyncMock()):
        results = await featured_artist_jobs.select_candidates_for_week(
            db, week_start=date(2026, 5, 4), n=5
        )

    assert len(results) == 1
    assert results[0]["artist_id"] == "artist-uuid-1"
    # new_artist_bonus + engagement(1.0) + rank(1.0) 포함 → score > 0
    assert results[0]["composite_score"] > 0
    reasoning = results[0]["reasoning"]
    assert "engagement" in reasoning
    assert "rank" in reasoning
    assert "new_artist_bonus" in reasoning


# ── 2. UNIQUE INDEX 중복 방지 — ON CONFLICT DO NOTHING ───────────────────────

@pytest.mark.asyncio
async def test_insert_candidates_on_conflict_do_nothing():
    """동일 artist_id + week_start 중복 INSERT → ON CONFLICT DO NOTHING."""
    db = AsyncMock()
    # 첫 번째 INSERT: RETURNING id 반환 (삽입 성공)
    # 두 번째 INSERT: RETURNING 없음 (충돌 무시)
    call_count = 0
    def execute_side_effect(*args, **kwargs):
        nonlocal call_count
        call_count += 1
        mock = MagicMock()
        mock.fetchone.return_value = ("new-uuid",) if call_count == 1 else None
        return mock

    db.execute = AsyncMock(side_effect=execute_side_effect)
    db.commit = AsyncMock()

    candidates = [{"artist_id": "artist-1", "composite_score": 0.8, "reasoning": {}}]
    week = date(2026, 5, 4)

    count1 = await featured_artist_jobs.insert_candidates(db, candidates, week)
    count2 = await featured_artist_jobs.insert_candidates(db, candidates, week)

    assert count1 == 1
    assert count2 == 0  # 충돌로 INSERT 없음


# ── 3. 후보 < 3 시 graceful — Slack mock ────────────────────────────────────

@pytest.mark.asyncio
async def test_low_candidate_alert_triggered():
    """후보 < 3명 시 _notify_admin_low_candidates 호출 확인."""
    db = AsyncMock()
    # 후보 1명만 반환
    mock_row = MagicMock(
        artist_id="artist-1",
        artist_rank=90.0,
        follower_count=50,
        region="KR",
        genre="oil",
        engagement_score=50.0,
        sponsor_count=0,
    )
    db.execute = AsyncMock(return_value=MagicMock(fetchall=lambda: [mock_row]))

    with patch(
        "app.services.featured_artist_jobs._notify_admin_low_candidates",
        new=AsyncMock()
    ) as mock_notify:
        results = await featured_artist_jobs.select_candidates_for_week(
            db, week_start=date(2026, 5, 4), n=5
        )
        mock_notify.assert_called_once()
        call_args = mock_notify.call_args
        assert call_args.args[1] < featured_artist_jobs._LOW_CANDIDATE_THRESHOLD


# ── 4. artist_index 미가용 fallback ──────────────────────────────────────────

@pytest.mark.asyncio
async def test_artist_index_unavailable_fallback():
    """DB 쿼리 예외 발생 시 빈 리스트 반환 + 오류 미전파."""
    db = AsyncMock()
    db.execute = AsyncMock(side_effect=Exception("artist_index table not found"))

    with patch(
        "app.services.featured_artist_jobs._notify_admin_low_candidates",
        new=AsyncMock()
    ):
        results = await featured_artist_jobs.select_candidates_for_week(
            db, week_start=date(2026, 5, 4)
        )

    assert results == []


# ── 5. 다양성 분산 검증 — 장르 ≥ 2종 ────────────────────────────────────────

@pytest.mark.asyncio
async def test_diversity_mmr_genre_variety():
    """상위 5명 선정 시 장르 ≥ 2종 다양성 보정 동작 확인."""
    # 5명 모두 같은 장르: MMR 패널티로 상위 점수 분산
    scored = [
        {"artist_id": f"a-{i}", "composite_score": float(10 - i),
         "reasoning": {"genre": "oil", "region": "KR", "sponsor_count": 0}}
        for i in range(10)
    ]
    result = featured_artist_jobs._apply_diversity_mmr(scored, pool_size=10)

    # 동일 장르 반복 → 패널티로 점수 감소 확인
    scores = [r["composite_score"] for r in result]
    # 첫 번째는 패널티 없음, 두 번째 이상은 감소
    assert scores[0] >= scores[1]


# ── 6. 신진작가 우선 선정 — sponsor_count=0 보너스 ──────────────────────────

@pytest.mark.asyncio
async def test_new_artist_bonus_prioritized():
    """후원자 0명 작가가 후원자 있는 작가보다 높은 점수 획득."""
    db = AsyncMock()
    rows = [
        MagicMock(artist_id="new-artist", artist_rank=80.0, follower_count=100,
                  region="VN", genre="digital", engagement_score=40.0, sponsor_count=0),
        MagicMock(artist_id="established", artist_rank=90.0, follower_count=800,
                  region="KR", genre="oil", engagement_score=50.0, sponsor_count=5),
    ]
    db.execute = AsyncMock(return_value=MagicMock(fetchall=lambda: rows))
    db.commit = AsyncMock()

    with patch("app.services.featured_artist_jobs._notify_admin_low_candidates", new=AsyncMock()):
        results = await featured_artist_jobs.select_candidates_for_week(
            db, week_start=date(2026, 5, 4), n=5
        )

    new_artist = next(r for r in results if r["artist_id"] == "new-artist")
    assert new_artist["reasoning"]["new_artist_bonus"] > 0
```

### 8-2. Integration Tests: `tests/integration/test_featured_artist_admin.py` (4+ 케이스)

| # | 테스트 케이스 | 검증 내용 |
|---|-------------|---------|
| 1 | `GET /api/admin/featured-artist/candidates` — 권한 검증 | admin 토큰: 200, user 토큰: 403, 미인증: 401 |
| 2 | approve → publish 워크플로우 | pending → approved → published 순서 정상 동작, `featured_artists` 테이블 INSERT 확인 |
| 3 | reject 워크플로우 | pending → rejected + reason 저장, reasoning JSONB에 `reject_reason` 키 존재 |
| 4 | cross-admin 동시성 (race condition) | 두 admin이 동시에 같은 후보 approve 시도 → 첫 번째만 성공, 두 번째 409 |

---

## 9. KPI 측정 방법

| KPI | 측정 쿼리 / 방법 | 목표 |
|-----|----------------|:----:|
| admin 수락률 | `SELECT COUNT(*) FILTER (WHERE status='approved') / COUNT(*) FROM featured_artist_candidates WHERE week_start >= :last_4_weeks` | ≥ 70% |
| Featured Artist CTR | PostHog: `featured_artist_click` / `featured_artist_impression` | ≥ 15% |
| 신진작가 선정 비율 | `SELECT COUNT(*) FILTER (WHERE reasoning->>'new_artist_bonus' != '0') / COUNT(*) FROM featured_artist_candidates WHERE status IN ('approved','published')` | ≥ 70% |
| 후원 전환율 | PostHog funnel: `featured_artist_click` → `sponsor_created` | baseline |
| admin 검수 시간 | `AVG(reviewed_at - created_at)` WHERE status IN ('approved','rejected') | 정성 측정 |

---

## 10. 위험 & 완화

| 위험 | 영향 | 가능성 | 완화 방안 |
|------|:----:|:------:|---------|
| ML 스코어 어뷰징 (팔로워 매크로 등) | 높음 | 중간 | **autopublish OFF 정책** — admin 최종 승인 필수. reasoning JSONB로 이상치 감지 |
| 동일 작가 반복 선정 | 중간 | 중간 | 최근 4주 `featured_artists` 쿼리로 제외 |
| 후보 pool 부족 (신진작가 < 5명) | 중간 | 낮음 | follower_count < 1000으로 기준 완화 + Slack 알림 |
| `artist_index` 미가용 | 중간 | 낮음 | follower_count 기반 정렬 fallback |
| `post_engagement_cache` 미가용 | 낮음 | 낮음 | 정적 like_count 기반 fallback |
| `user_embeddings` 미생성 | 낮음 | 낮음 | 다양성 보정 skip (MMR 비적용) |
| alembic 0082 → 0083 체인 충돌 | 중간 | 낮음 | K-4/K-7 병렬 진행 후 순차 merge (Wave B 완료 후) |

---

## 11. 위임 Agent

| Agent | 담당 작업 | 산출물 |
|-------|----------|--------|
| `bkend-expert` | alembic 0082 + `featured_artist_jobs.py` + `main.py` 22번째 cron 등록 + 4 admin endpoints + unit/integration tests | `0082_featured_artist_candidates.py`, `featured_artist_jobs.py`, `tests/unit/test_featured_artist_jobs.py`, `tests/integration/test_featured_artist_admin.py` |
| `frontend-architect` | `/admin/featured-artist-queue` 페이지 + i18n 5 locale 키 추가 (선택, K-4 backend 완료 후) | `src/app/admin/featured-artist-queue/page.tsx` |

### 병렬 실행 가능 범위

```
[K-4와 병렬 가능]
  bkend-expert: K-4 (alembic 0082 + jobs + endpoints)
  bkend-expert: K-7 (alembic 0083 + ai_curation_jobs) ← Wave B 병렬

[순차 (K-4 내부)]
  1. alembic 0082 작성
  2. featured_artist_jobs.py 작성
  3. main.py 22번째 cron 등록
  4. 4 admin endpoints 구현
  5. unit tests 작성 + 통과
  6. integration tests 작성 + 통과

[alembic chain]
  K-4 완료: alembic head = 0082
  K-7 완료: alembic head = 0083 (down_revision="0082")
  → K-4/K-7 완료 후 single head 0083 확인
```

---

## Version History

| 버전 | 날짜 | 변경사항 | 작성자 |
|------|------|---------|--------|
| 0.1 | 2026-05-05 | K-4 design 초기 작성. alembic 0082, featured_artist_jobs.py, 4 admin endpoints, 6+4 tests, Mock fallback 매트릭스 포함. autopublish OFF 정책, 후보 5명, 다양성 MMR, funnel 좌측(후원자 0) 보너스. | itpe-ince (Claude Sonnet 4.6) |

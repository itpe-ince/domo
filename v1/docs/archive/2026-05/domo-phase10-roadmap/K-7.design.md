---
template: design
version: 1.0
feature: ai-curation-collection
phase: 10 / K-7
date: 2026-05-05
author: itpe-ince (Claude Sonnet 4.6)
project: domo
project_version: v1
parent_plan: domo-phase10-roadmap.plan.md
alembic: "0083"
status: Draft
---

# Phase 10 K-7 Design — AI 큐레이션 컬렉션 (Editor's Pick 자동 생성)

> **Summary**: tuzigroup LLM Gateway(gemma4-e4b)가 작품 메타 데이터(ai_caption + 장르 태그)를
> 분석해 주제별 컬렉션 제목과 설명을 자동 생성한다. L-A `post_embeddings` K-means 클러스터링으로
> 주제를 발견하고, L-F `translation_cache`를 재사용해 5 locale 번역 비용을 최소화한다.
> 주 1회 cron(월요일 09:00 UTC)으로 5개 컬렉션을 생성 → admin 검수 → 공개 노출한다.
> 이는 README 비전 "스토리텔링 hub"와 "히스토리를 두세 개 만든다"를 직접 구현한다.

---

## README 비전 연결

> "컬렉터들한테는 회비 1년에 10분씩" — Editor's Pick 주제별 컬렉션으로 컬렉터 탐색 경험을
> 풍부하게 만들어 구독 유지 가치를 높인다.

> "히스토리를 두세 개 만든다" — AI 큐레이션 컬렉션 제목/설명 자동 생성으로
> 언론/SNS 확산 가능한 "발견 스토리"를 매주 자동화한다.

> "전 세계 아티스트들의 인덱스를 만들고 싶음" — 신진 작가 클러스터링을 통해
> 발견되기 어려운 작가를 주제 컬렉션으로 조명해 글로벌 인덱스를 능동 큐레이션한다.

K-5(LLM 도슨트, 작품 단위 해설)가 개별 작품의 스토리를 만든다면, K-7은 여러 작품을
하나의 주제로 묶어 "컬렉션 스토리"를 만든다. 두 기능이 결합해 Domo를 스토리텔링 허브로
완성한다.

---

## 1. 목표 & Acceptance Criteria

### 1-1. 목표

| # | 목표 | 근거 |
|---|------|------|
| 1 | 주 1회 주제별 컬렉션 5개 자동 생성 | 컬렉터·일반 관람자 탐색 경험 풍부화 |
| 2 | LLM이 컬렉션 제목/설명을 한국어로 자동 생성 | 편집 인력 없이 고품질 큐레이션 자동화 |
| 3 | L-F translation_cache 재사용으로 5 locale 번역 비용 최소화 | LLM 비용 일 $5 한도 유지 |
| 4 | L-A post_embeddings K-means 클러스터링으로 주제 자동 발견 | 사람이 주제를 정의하지 않아도 됨 |
| 5 | admin 검수 후 공개 노출 (autopublish OFF) | ML 생성 콘텐츠 품질 안전망 확보 |
| 6 | LLM/sklearn 미설정 시 graceful fallback | CI/개발 환경에서 ERROR 없이 동작 |

### 1-2. Acceptance Criteria

- [ ] alembic 0083 적용 후 `ai_collections` + `ai_collection_posts` 테이블 생성 확인
- [ ] `ai_curation_cron_loop()` — 주 1회 월요일 09:00 UTC 동작, `AI_CURATION_WORKER_ENABLED` guard 확인
- [ ] 컬렉션 제목/설명 한국어 원본 + JSONB 5 locale(en/ja/zh/es) 모두 생성 확인
- [ ] admin 큐에서 승인/편집/거부 API 동작 확인 (generating → published / archived)
- [ ] `GET /api/collections` 공개 엔드포인트 응답 (published 컬렉션만, 페이지네이션)
- [ ] `GET /api/collections/{id}` — ai_collection_posts position 순 작품 리스트 반환
- [ ] `/explore/collections` 페이지에서 5 locale 자동 매칭 노출 확인
- [ ] LLM Gateway 미설정 시 status='generating' 유지 + log WARNING 출력 (에러 없음)
- [ ] sklearn 미설치 시 metadata-based grouping fallback 동작 확인
- [ ] LLM 일 $5 한도 guard: 한도 초과 시 cron skip + 로그 출력 확인
- [ ] UNIQUE INDEX (theme, week_start) 위반 시 409 중복 방지 동작 확인
- [ ] `tests/unit/test_ai_curation_jobs.py` 8+ tests green
- [ ] `tests/integration/test_collections_endpoints.py` 5+ tests green

### 1-3. KPIs

| 지표 | 목표 | 측정 도구 |
|------|------|---------|
| 컬렉션 페이지 CTR | `/explore` → 컬렉션 클릭 ≥ 15% | PostHog `collection_click` 이벤트 |
| admin 컬렉션 승인율 | ≥ 70% (자동 생성 품질 기준) | DB: published / (published + archived) |
| LLM 비용 | 주당 $35 이하 (일 $5 × 7일) | tuzigroup 사용량 API |
| translation_cache 히트율 | ≥ 60% | translation_cache.hit_count 집계 |
| 작가 발굴 가속 | ai_collection_posts 포함 신진작가 후원 전환율 | PostHog `sponsor_created` funnel |

---

## 2. Architecture

### 2-1. 데이터 플로우

```
[매주 월요일 09:00 UTC] ai_curation_cron_loop()
        │
        ▼
1단계: post_embeddings 클러스터링
   ├─ sklearn KMeans(k=5) — 설치된 경우
   └─ fallback: 장르 태그 기반 metadata grouping
        │
        ▼
2단계: 클러스터별 주제 분석
   ├─ ai_caption 텍스트 TF-IDF 요약 (top 키워드 추출)
   ├─ 장르 태그 집계 (가장 빈도 높은 태그)
   └─ 신진작가 비율 계산 (팔로워 < 100)
        │
        ▼
3단계: LLM 호출 — 컬렉션 메타 생성
   ├─ 입력: 클러스터 대표 포스트 ai_caption × 5~8개 + 주제 키워드
   ├─ 출력: 컬렉션 제목(한국어, ≤ 30자) + 설명(한국어, 2~3문장)
   └─ LLM 미설정 시 → status='generating' 유지 + log
        │
        ▼
4단계: L-F translation_cache 번역 (5 locale)
   ├─ ko 원본 → en/ja/zh/es 각각 캐시 조회
   ├─ cache hit → LLM 재호출 없음
   └─ cache miss → LLM 번역 후 cache INSERT
        │
        ▼
5단계: DB 저장
   ├─ ai_collections INSERT (status='generating')
   ├─ 대표 작품 선정 (cover_post_id: ML 스코어 상위 1개)
   └─ ai_collection_posts INSERT (top 10~20개, position 순)
        │
        ▼
[admin 검수]
   ├─ GET /api/admin/collections/queue — 검수 대기 목록
   ├─ POST /api/admin/collections/{id}/publish — 공개
   └─ POST /api/admin/collections/{id}/archive — 보류
        │
        ▼
[공개 노출]
   ├─ GET /api/collections — 활성 컬렉션 목록
   ├─ /explore/collections — 컬렉션 카드 리스트 (Frontend)
   └─ /explore/collections/[id] — 컬렉션 상세 + 작품 그리드 (Frontend)
```

### 2-2. 기존 인프라 재사용 (재발명 X)

| 기존 인프라 | K-7 활용 방식 |
|------------|:------------|
| L-A `post_embeddings` (alembic 0066) | K-means 클러스터링 입력 — pgvector 128차원 벡터 |
| L-A `embedding_jobs.py` | K-7에서 직접 DB SELECT (신규 코드 불필요) |
| L-F `translation_cache` (alembic 0071) | 컬렉션 제목/설명 5 locale 번역 캐싱 |
| L-F `translation_cache.py` 서비스 | `get_cached_translation()` / `set_cached_translation()` 재사용 |
| K-3 `ai_caption` (alembic 0078) | 클러스터링 텍스트 feature 소스 |
| K-3/K-5 `llm_gateway.py` | `LLMGatewayClient` 직접 재사용 |
| Phase 7 G'-7 admin 검수 큐 패턴 | admin API 설계 계승 |

---

## 3. Database Schema (alembic 0083)

### 파일: `v1/backend/alembic/versions/0083_ai_collections.py`

```python
"""alembic 0083 — AI 큐레이션 컬렉션 (ai_collections, ai_collection_posts)

Phase 10 K-7: Editor's Pick 자동 생성.
LLM 큐레이션 컬렉션 + 작품 M:N 매핑 테이블.

down_revision: 0082_featured_artist_candidates (K-4)
"""
from __future__ import annotations

from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = "0083_ai_collections"
down_revision = "0082_featured_artist_candidates"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # ai_collections: 주제별 AI 큐레이션 컬렉션
    op.execute("""
        CREATE TYPE ai_collection_status AS ENUM
            ('generating', 'published', 'archived')
    """)

    op.create_table(
        "ai_collections",
        sa.Column("id", postgresql.UUID(as_uuid=True),
                  server_default=sa.text("uuid_generate_v4()"), primary_key=True),
        sa.Column("week_start", sa.Date(), nullable=False,
                  comment="해당 주 월요일 날짜 (ISO week 기준)"),
        sa.Column("theme", sa.VARCHAR(100), nullable=False,
                  comment="주제 식별자 예: emerging_painters, digital_art_pioneers"),
        sa.Column("title", sa.VARCHAR(200), nullable=True,
                  comment="LLM 생성 한국어 원본 제목 (≤ 30자)"),
        sa.Column("description", sa.TEXT(), nullable=True,
                  comment="LLM 생성 한국어 원본 설명 (2~3문장)"),
        sa.Column("title_translations", postgresql.JSONB(), nullable=False,
                  server_default=sa.text("'{}'::jsonb"),
                  comment='5 locale 번역 {"en": "...", "ja": "...", "zh": "...", "es": "..."}'),
        sa.Column("description_translations", postgresql.JSONB(), nullable=False,
                  server_default=sa.text("'{}'::jsonb"),
                  comment="5 locale 설명 번역"),
        sa.Column("cover_post_id", postgresql.UUID(as_uuid=True), nullable=True,
                  comment="대표 작품 ID (썸네일 소스)"),
        sa.Column("status", postgresql.ENUM(
            "generating", "published", "archived",
            name="ai_collection_status", create_type=False,
        ), nullable=False, server_default="generating"),
        sa.Column("cluster_k", sa.Integer(), nullable=True,
                  comment="KMeans k값 (클러스터링에 사용된 k)"),
        sa.Column("llm_model_version", sa.VARCHAR(50), nullable=True,
                  comment="컬렉션 생성에 사용한 LLM 모델 식별자"),
        sa.Column("admin_note", sa.TEXT(), nullable=True,
                  comment="admin 검수 메모"),
        sa.Column("generated_at", postgresql.TIMESTAMPTZ(), nullable=False,
                  server_default=sa.text("now()")),
        sa.Column("published_at", postgresql.TIMESTAMPTZ(), nullable=True),
        sa.ForeignKeyConstraint(
            ["cover_post_id"], ["posts.id"], ondelete="SET NULL"
        ),
    )

    # UNIQUE: 같은 주제는 같은 주에 1개만
    op.create_index(
        "uq_ai_collections_theme_week",
        "ai_collections",
        ["theme", "week_start"],
        unique=True,
    )
    # 공개 목록 정렬 인덱스
    op.create_index(
        "ix_ai_collections_status_published",
        "ai_collections",
        ["status", "published_at"],
        postgresql_ops={"published_at": "DESC NULLS LAST"},
    )
    # 생성일 인덱스 (admin 검수 큐 정렬)
    op.create_index(
        "ix_ai_collections_generated_at",
        "ai_collections",
        ["generated_at"],
    )

    # ai_collection_posts: 컬렉션-작품 M:N 매핑
    op.create_table(
        "ai_collection_posts",
        sa.Column("id", postgresql.UUID(as_uuid=True),
                  server_default=sa.text("uuid_generate_v4()"), primary_key=True),
        sa.Column("collection_id", postgresql.UUID(as_uuid=True), nullable=False),
        sa.Column("post_id", postgresql.UUID(as_uuid=True), nullable=False),
        sa.Column("position", sa.Integer(), nullable=False,
                  comment="컬렉션 내 노출 순서 (1-indexed)"),
        sa.Column("ml_score", sa.Float(), nullable=True,
                  comment="클러스터링 시 ML 스코어 (대표성 지표)"),
        sa.ForeignKeyConstraint(
            ["collection_id"], ["ai_collections.id"], ondelete="CASCADE"
        ),
        sa.ForeignKeyConstraint(
            ["post_id"], ["posts.id"], ondelete="CASCADE"
        ),
    )

    # 컬렉션별 position 정렬 인덱스
    op.create_index(
        "ix_ai_collection_posts_collection_position",
        "ai_collection_posts",
        ["collection_id", "position"],
    )
    # post 역방향 인덱스 (특정 작품이 포함된 컬렉션 조회)
    op.create_index(
        "ix_ai_collection_posts_post_id",
        "ai_collection_posts",
        ["post_id"],
    )


def downgrade() -> None:
    op.drop_index("ix_ai_collection_posts_post_id", "ai_collection_posts")
    op.drop_index("ix_ai_collection_posts_collection_position", "ai_collection_posts")
    op.drop_table("ai_collection_posts")

    op.drop_index("ix_ai_collections_generated_at", "ai_collections")
    op.drop_index("ix_ai_collections_status_published", "ai_collections")
    op.drop_index("uq_ai_collections_theme_week", "ai_collections")
    op.drop_table("ai_collections")

    op.execute("DROP TYPE IF EXISTS ai_collection_status")
```

### 스키마 요약

#### `ai_collections`

| 컬럼 | 타입 | 제약 | 설명 |
|------|------|------|------|
| `id` | UUID PK | uuid_generate_v4() | 기본키 |
| `week_start` | DATE | NOT NULL | 해당 주 월요일 날짜 |
| `theme` | VARCHAR(100) | NOT NULL | 주제 식별자 |
| `title` | VARCHAR(200) | NULL | LLM 생성 한국어 원본 제목 |
| `description` | TEXT | NULL | LLM 생성 한국어 원본 설명 |
| `title_translations` | JSONB | NOT NULL DEFAULT '{}' | 5 locale 번역 |
| `description_translations` | JSONB | NOT NULL DEFAULT '{}' | 5 locale 설명 번역 |
| `cover_post_id` | UUID FK | NULL, SET NULL | 대표 작품 (posts.id) |
| `status` | ENUM | NOT NULL DEFAULT 'generating' | generating / published / archived |
| `cluster_k` | INTEGER | NULL | 사용된 KMeans k값 |
| `llm_model_version` | VARCHAR(50) | NULL | LLM 모델 식별자 |
| `admin_note` | TEXT | NULL | admin 검수 메모 |
| `generated_at` | TIMESTAMPTZ | NOT NULL DEFAULT now() | 생성 시각 |
| `published_at` | TIMESTAMPTZ | NULL | 공개 시각 |

#### `ai_collection_posts`

| 컬럼 | 타입 | 제약 | 설명 |
|------|------|------|------|
| `id` | UUID PK | uuid_generate_v4() | 기본키 |
| `collection_id` | UUID FK | NOT NULL, CASCADE | ai_collections.id |
| `post_id` | UUID FK | NOT NULL, CASCADE | posts.id |
| `position` | INTEGER | NOT NULL | 노출 순서 (1-indexed) |
| `ml_score` | FLOAT | NULL | 대표성 ML 스코어 |

#### 인덱스

| 인덱스 | 컬럼 | 타입 | 목적 |
|--------|------|------|------|
| `uq_ai_collections_theme_week` | (theme, week_start) | UNIQUE | 같은 주 중복 컬렉션 방지 |
| `ix_ai_collections_status_published` | (status, published_at DESC) | B-Tree | 공개 목록 정렬 |
| `ix_ai_collections_generated_at` | generated_at | B-Tree | admin 큐 정렬 |
| `ix_ai_collection_posts_collection_position` | (collection_id, position) | B-Tree | 컬렉션 내 순서 조회 |
| `ix_ai_collection_posts_post_id` | post_id | B-Tree | 작품 역방향 조회 |

---

## 4. Service Layer

### 4-1. `app/services/ai_curation_jobs.py` (신규)

```python
"""AI 큐레이션 컬렉션 자동 생성 — Phase 10 K-7.

주 1회 월요일 09:00 UTC cron으로 Editor's Pick 컬렉션 5개를 자동 생성한다.

파이프라인:
  1단계: post_embeddings K-means 클러스터링 (sklearn) → 주제 그룹 발견
  2단계: 클러스터별 주제 분석 (ai_caption 텍스트 + 장르 태그)
  3단계: LLM Gateway → 컬렉션 제목/설명 한국어 생성
  4단계: L-F translation_cache → 5 locale 번역
  5단계: ai_collections + ai_collection_posts DB INSERT

환경변수:
  AI_CURATION_WORKER_ENABLED: false로 설정 시 cron 미등록 (default: true)
  AI_CURATION_DAILY_BUDGET_USD: LLM 일 비용 한도 (default: 5.0)
  AI_CURATION_COLLECTIONS_PER_WEEK: 주당 생성 컬렉션 수 (default: 5)
  AI_CURATION_POSTS_PER_COLLECTION: 컬렉션당 작품 수 (default: 15)

Graceful fallback:
  - sklearn 미설치 → metadata-based grouping (장르 태그 기반)
  - LLM Gateway 미설정 → status='generating' 유지 + log WARNING
  - translation_cache 미가용 → 한국어 원본만 저장 (빈 translations)
  - LLM 비용 한도 초과 → cron skip + log WARNING
"""
from __future__ import annotations

import asyncio
import logging
import os
from datetime import date, datetime, timedelta, timezone
from typing import Any

from sqlalchemy import text

from app.db.session import AsyncSessionLocal
from app.services.llm_gateway import LLMGatewayClient
from app.services.translation_cache import get_cached_translation, set_cached_translation

log = logging.getLogger(__name__)

_COLLECTIONS_PER_WEEK = int(os.getenv("AI_CURATION_COLLECTIONS_PER_WEEK", "5"))
_POSTS_PER_COLLECTION = int(os.getenv("AI_CURATION_POSTS_PER_COLLECTION", "15"))
_DAILY_BUDGET_USD = float(os.getenv("AI_CURATION_DAILY_BUDGET_USD", "5.0"))
_TARGET_LOCALES = ["en", "ja", "zh", "es"]

# 주제 후보 (클러스터가 비어있을 경우 fallback 레이블)
_THEME_CANDIDATES = [
    "emerging_painters",
    "digital_art_pioneers",
    "southeast_asia_artists",
    "abstract_expressionism",
    "photo_realism",
    "street_art_new_wave",
    "mixed_media_experiments",
    "watercolor_renaissance",
]


# ─────────────────────────────────────────────────────────────────────────────
# 클러스터링
# ─────────────────────────────────────────────────────────────────────────────

async def _load_post_embeddings(db, limit: int = 2000) -> list[dict]:
    """post_embeddings에서 최근 포스트 임베딩 로드.

    Returns list of {"post_id": str, "embedding": list[float],
                      "ai_caption": str | None, "tags": list[str]}
    """
    result = await db.execute(text("""
        SELECT pe.post_id,
               pe.embedding::text AS embedding_text,
               p.ai_caption,
               p.tags
        FROM post_embeddings pe
        JOIN posts p ON p.id = pe.post_id
        WHERE p.status = 'published'
        ORDER BY pe.updated_at DESC
        LIMIT :lim
    """), {"lim": limit})
    rows = result.fetchall()

    posts = []
    for r in rows:
        try:
            # vector(128) 텍스트 파싱: "[0.1, 0.2, ...]"
            emb_str = r.embedding_text.strip("[]")
            embedding = [float(x) for x in emb_str.split(",")]
        except Exception:
            embedding = [0.0] * 128
        posts.append({
            "post_id": str(r.post_id),
            "embedding": embedding,
            "ai_caption": r.ai_caption or "",
            "tags": r.tags or [],
        })
    return posts


def _cluster_by_sklearn(posts: list[dict], k: int) -> list[list[dict]]:
    """sklearn KMeans 클러스터링. 설치되지 않은 경우 ImportError 발생."""
    import numpy as np
    from sklearn.cluster import KMeans  # type: ignore

    embeddings = np.array([p["embedding"] for p in posts], dtype=np.float32)
    km = KMeans(n_clusters=k, random_state=42, n_init=10)
    labels = km.fit_predict(embeddings)

    clusters: list[list[dict]] = [[] for _ in range(k)]
    for post, label in zip(posts, labels):
        clusters[label].append(post)
    return clusters


def _cluster_by_metadata(posts: list[dict], k: int) -> list[list[dict]]:
    """sklearn 미설치 시 fallback: 장르 태그 기반 grouping.

    태그 없는 포스트는 'uncategorized' 버킷으로 분류.
    """
    from collections import defaultdict

    tag_groups: dict[str, list[dict]] = defaultdict(list)
    for post in posts:
        primary_tag = post["tags"][0] if post["tags"] else "uncategorized"
        tag_groups[primary_tag].append(post)

    # k개 클러스터로 제한 (크기 순 상위 k개 태그 선택)
    sorted_groups = sorted(tag_groups.values(), key=len, reverse=True)
    return sorted_groups[:k]


def _cluster_posts(posts: list[dict], k: int) -> tuple[list[list[dict]], bool]:
    """K-means 클러스터링 시도. sklearn 미설치 시 metadata fallback.

    Returns: (clusters, used_sklearn)
    """
    try:
        clusters = _cluster_by_sklearn(posts, k)
        log.info("ai_curation: sklearn KMeans clustering k=%d, posts=%d", k, len(posts))
        return clusters, True
    except ImportError:
        log.warning(
            "ai_curation: sklearn not installed — falling back to metadata-based grouping"
        )
        clusters = _cluster_by_metadata(posts, k)
        return clusters, False


# ─────────────────────────────────────────────────────────────────────────────
# LLM 컬렉션 메타 생성
# ─────────────────────────────────────────────────────────────────────────────

def _build_collection_prompt(
    captions: list[str],
    tags: list[str],
    previous_titles: list[str],
) -> str:
    """LLM에 전달할 컬렉션 제목/설명 생성 프롬프트."""
    captions_text = "\n".join(f"- {c}" for c in captions[:8] if c)
    tags_text = ", ".join(sorted(set(tags))[:10])
    prev_text = "\n".join(f"- {t}" for t in previous_titles[-10:]) if previous_titles else "없음"

    return f"""당신은 예술 큐레이터입니다. 다음 작품 설명들을 분석해 
하나의 주제로 묶는 컬렉션 제목과 소개글을 한국어로 작성해주세요.

[작품 설명 샘플]
{captions_text}

[주요 태그]
{tags_text}

[이전 컬렉션 제목 목록 (반복 금지)]
{prev_text}

요구사항:
1. 제목: 30자 이내, 독자의 호기심을 자극하는 문구
2. 설명: 2~3문장, 이 컬렉션을 탐색해야 하는 이유 설명
3. 이전 제목과 유사한 표현 사용 금지
4. 형식: JSON {{ "title": "...", "description": "..." }}"""


async def _generate_collection_meta(
    llm: LLMGatewayClient,
    captions: list[str],
    tags: list[str],
    previous_titles: list[str],
) -> dict[str, str] | None:
    """LLM으로 컬렉션 제목/설명 생성.

    Returns {"title": str, "description": str} 또는 LLM 미설정 시 None.
    """
    if llm.is_mock:
        log.warning("ai_curation: LLM Gateway not configured — skipping meta generation")
        return None

    prompt = _build_collection_prompt(captions, tags, previous_titles)
    try:
        result = await llm.generate_interview(prompt, max_tokens=300, temperature=0.8)
        content = result.get("content", "")

        # JSON 파싱
        import json
        import re
        json_match = re.search(r"\{[^{}]+\}", content, re.DOTALL)
        if json_match:
            parsed = json.loads(json_match.group())
            return {
                "title": parsed.get("title", "")[:200],
                "description": parsed.get("description", ""),
            }
    except Exception as exc:
        log.warning("ai_curation: LLM meta generation failed: %s", exc)
    return None


# ─────────────────────────────────────────────────────────────────────────────
# 번역 (L-F translation_cache 재사용)
# ─────────────────────────────────────────────────────────────────────────────

async def _translate_texts(
    db,
    llm: LLMGatewayClient,
    texts: dict[str, str],  # {"title": ko_title, "description": ko_desc}
) -> tuple[dict[str, str], dict[str, str]]:
    """한국어 제목/설명 → 5 locale 번역.

    L-F translation_cache를 먼저 조회하고, miss 시에만 LLM 번역을 호출한다.

    Returns: (title_translations, description_translations)
      각각 {"en": "...", "ja": "...", "zh": "...", "es": "..."}
    """
    title_translations: dict[str, str] = {}
    desc_translations: dict[str, str] = {}

    for locale in _TARGET_LOCALES:
        # 제목 번역
        if texts.get("title"):
            cached_title = await get_cached_translation(
                db, texts["title"], "ko", locale
            )
            if cached_title:
                title_translations[locale] = cached_title
            elif not llm.is_mock:
                try:
                    trans_result = await llm.generate_interview(
                        f"다음 한국어 텍스트를 {locale} 언어로 번역해주세요. "
                        f"번역 결과만 출력하세요:\n{texts['title']}",
                        max_tokens=100,
                        temperature=0.3,
                    )
                    translated = trans_result.get("content", "").strip()
                    if translated:
                        title_translations[locale] = translated
                        await set_cached_translation(
                            db, texts["title"], "ko", locale, translated,
                        )
                except Exception as exc:
                    log.warning("ai_curation: title translation to %s failed: %s", locale, exc)

        # 설명 번역
        if texts.get("description"):
            cached_desc = await get_cached_translation(
                db, texts["description"], "ko", locale
            )
            if cached_desc:
                desc_translations[locale] = cached_desc
            elif not llm.is_mock:
                try:
                    trans_result = await llm.generate_interview(
                        f"다음 한국어 텍스트를 {locale} 언어로 번역해주세요. "
                        f"번역 결과만 출력하세요:\n{texts['description']}",
                        max_tokens=300,
                        temperature=0.3,
                    )
                    translated = trans_result.get("content", "").strip()
                    if translated:
                        desc_translations[locale] = translated
                        await set_cached_translation(
                            db, texts["description"], "ko", locale, translated,
                        )
                except Exception as exc:
                    log.warning("ai_curation: desc translation to %s failed: %s", locale, exc)

    return title_translations, desc_translations


# ─────────────────────────────────────────────────────────────────────────────
# 컬렉션 생성 메인
# ─────────────────────────────────────────────────────────────────────────────

async def generate_collections_for_week(db, week_start: date) -> list[str]:
    """주어진 week_start에 대한 AI 큐레이션 컬렉션을 생성한다.

    Returns: 생성된 collection_id 목록.

    파이프라인:
      1. post_embeddings 로드
      2. K-means 클러스터링 (k=5)
      3. 클러스터별 LLM 메타 생성
      4. translation_cache 5 locale 번역
      5. ai_collections + ai_collection_posts INSERT
    """
    import json

    posts = await _load_post_embeddings(db, limit=2000)
    if len(posts) < _COLLECTIONS_PER_WEEK:
        log.warning(
            "ai_curation: not enough posts (%d) for clustering — skipping", len(posts)
        )
        return []

    k = _COLLECTIONS_PER_WEEK
    clusters, used_sklearn = _cluster_posts(posts, k)

    # 이전 컬렉션 제목 로드 (클리셰 반복 방지)
    prev_result = await db.execute(text("""
        SELECT title FROM ai_collections
        WHERE week_start >= :cutoff
        ORDER BY generated_at DESC
        LIMIT 20
    """), {"cutoff": week_start - timedelta(weeks=4)})
    previous_titles = [r.title for r in prev_result.fetchall() if r.title]

    llm = LLMGatewayClient()
    created_ids: list[str] = []

    for idx, cluster in enumerate(clusters):
        if not cluster:
            continue

        theme = _THEME_CANDIDATES[idx % len(_THEME_CANDIDATES)]

        # 중복 확인 (UNIQUE INDEX 위반 방지)
        dup_check = await db.execute(text("""
            SELECT id FROM ai_collections
            WHERE theme = :theme AND week_start = :ws
            LIMIT 1
        """), {"theme": theme, "week_start": week_start})
        if dup_check.fetchone():
            log.info("ai_curation: theme=%s week=%s already exists — skip", theme, week_start)
            continue

        # 대표 포스트 (ml_score 기준 정렬: 없으면 순서 유지)
        top_posts = cluster[:_POSTS_PER_COLLECTION]
        captions = [p["ai_caption"] for p in top_posts if p["ai_caption"]]
        tags = [tag for p in top_posts for tag in p["tags"]]

        # LLM 메타 생성
        meta = await _generate_collection_meta(llm, captions, tags, previous_titles)

        title = meta["title"] if meta else None
        description = meta["description"] if meta else None
        title_translations: dict = {}
        desc_translations: dict = {}

        if meta:
            title_translations, desc_translations = await _translate_texts(
                db, llm, {"title": title, "description": description}
            )
            previous_titles.append(title)  # 다음 클러스터에서 반복 방지

        # cover_post_id: 첫 번째 포스트
        cover_post_id = top_posts[0]["post_id"] if top_posts else None

        # ai_collections INSERT
        insert_result = await db.execute(text("""
            INSERT INTO ai_collections
              (week_start, theme, title, description,
               title_translations, description_translations,
               cover_post_id, status, cluster_k, llm_model_version)
            VALUES
              (:ws, :theme, :title, :desc,
               :title_tr::jsonb, :desc_tr::jsonb,
               :cover::uuid, 'generating', :k, :model)
            RETURNING id
        """), {
            "ws": week_start,
            "theme": theme,
            "title": title,
            "desc": description,
            "title_tr": json.dumps(title_translations, ensure_ascii=False),
            "desc_tr": json.dumps(desc_translations, ensure_ascii=False),
            "cover": cover_post_id,
            "k": k if used_sklearn else None,
            "model": llm.model if not llm.is_mock else None,
        })
        collection_id = str(insert_result.fetchone().id)

        # ai_collection_posts INSERT
        for position, post in enumerate(top_posts, start=1):
            await db.execute(text("""
                INSERT INTO ai_collection_posts
                  (collection_id, post_id, position, ml_score)
                VALUES (:cid, :pid, :pos, :score)
            """), {
                "cid": collection_id,
                "pid": post["post_id"],
                "pos": position,
                "score": post.get("ml_score"),
            })

        await db.commit()
        created_ids.append(collection_id)
        log.info(
            "ai_curation: collection created id=%s theme=%s title=%s",
            collection_id, theme, title,
        )

    return created_ids


# ─────────────────────────────────────────────────────────────────────────────
# Cron loop (R-5 격리)
# ─────────────────────────────────────────────────────────────────────────────

async def ai_curation_cron_loop() -> None:
    """주 1회 AI 큐레이션 컬렉션 생성 cron.

    매주 월요일 09:00 UTC 실행.
    LLM 비용 일 $5 한도 초과 시 해당 주 skip.

    R-5 격리 패턴 준수:
      - 별도 파일 (ai_curation_jobs.py)
      - AsyncSessionLocal 독립 사용
      - 개별 try/except
    """
    while True:
        now = datetime.now(timezone.utc)
        # 다음 월요일 09:00 UTC 계산
        days_until_monday = (7 - now.weekday()) % 7 or 7
        next_run = now.replace(
            hour=9, minute=0, second=0, microsecond=0
        ) + timedelta(days=days_until_monday)
        wait_seconds = (next_run - now).total_seconds()

        log.info("ai_curation_cron: next run at %s (%.0fs)", next_run, wait_seconds)
        await asyncio.sleep(wait_seconds)

        # 비용 한도 확인 (환경변수)
        daily_budget = float(os.getenv("AI_CURATION_DAILY_BUDGET_USD", "5.0"))
        if daily_budget <= 0:
            log.warning("ai_curation_cron: AI_CURATION_DAILY_BUDGET_USD=0 — skipping")
            continue

        run_date = datetime.now(timezone.utc).date()
        # week_start = 해당 주 월요일
        week_start = run_date - timedelta(days=run_date.weekday())

        async with AsyncSessionLocal() as db:
            try:
                created = await generate_collections_for_week(db, week_start)
                log.info(
                    "ai_curation_cron: week=%s created %d collections: %s",
                    week_start, len(created), created,
                )
            except Exception as exc:
                log.warning("ai_curation_cron: error for week=%s: %s", week_start, exc)
```

### 4-2. `app/main.py` — cron worker 등록 (23번째)

```python
# main.py — 기존 22개 worker 이후 추가
from app.services.ai_curation_jobs import ai_curation_cron_loop
import os as _os

if _os.getenv("AI_CURATION_WORKER_ENABLED", "true").lower() != "false":
    # 23rd cron worker — ai_curation (R-5 isolated, weekly Monday 09:00 UTC, K-7)
    ai_curation_task = asyncio.create_task(ai_curation_cron_loop())
else:
    ai_curation_task = None
```

---

## 5. API Endpoints

### 5-1. 공개 API

#### `GET /api/collections`

활성(published) 컬렉션 목록. 인증 불필요.

**Query Parameters**

| 파라미터 | 타입 | 기본값 | 설명 |
|---------|------|--------|------|
| `page` | int | 1 | 페이지 번호 |
| `limit` | int | 10 | 페이지 크기 (최대 20) |
| `locale` | string | ko | 번역 locale (ko/en/ja/zh/es) |

**Response 200**

```json
{
  "items": [
    {
      "id": "uuid",
      "week_start": "2026-05-04",
      "theme": "emerging_painters",
      "title": "이번 주 신진 페인터",
      "description": "새로운 감성의 페인터들이 등장했습니다...",
      "cover_image_url": "https://...",
      "post_count": 15,
      "published_at": "2026-05-05T09:30:00Z"
    }
  ],
  "total": 42,
  "page": 1,
  "limit": 10
}
```

**구현 메모**: locale 파라미터로 title_translations / description_translations JSONB에서
해당 locale 값을 반환한다. locale='ko' 또는 번역이 없으면 원본 title/description 반환.

---

#### `GET /api/collections/{id}`

컬렉션 상세 + 작품 리스트. 인증 불필요.

**Response 200**

```json
{
  "id": "uuid",
  "week_start": "2026-05-04",
  "theme": "emerging_painters",
  "title": "이번 주 신진 페인터",
  "description": "새로운 감성의 페인터들이 등장했습니다...",
  "cover_image_url": "https://...",
  "published_at": "2026-05-05T09:30:00Z",
  "posts": [
    {
      "position": 1,
      "post_id": "uuid",
      "title": "작품 제목",
      "thumbnail_url": "https://...",
      "author": { "id": "uuid", "name": "작가명" }
    }
  ]
}
```

**Response 404**: 컬렉션 없음 또는 published 상태 아님.

---

### 5-2. Admin API

모든 admin 엔드포인트는 `Authorization: Bearer {adminToken}` + admin RBAC 필요.

#### `GET /api/admin/collections/queue`

generating 상태 컬렉션 목록 (admin 검수 큐).

**Response 200**

```json
{
  "items": [
    {
      "id": "uuid",
      "week_start": "2026-05-04",
      "theme": "emerging_painters",
      "title": "이번 주 신진 페인터",
      "description": "새로운 감성의 페인터들이 등장했습니다...",
      "title_translations": { "en": "...", "ja": "...", "zh": "...", "es": "..." },
      "description_translations": { "en": "...", "ja": "...", "zh": "...", "es": "..." },
      "post_count": 15,
      "generated_at": "2026-05-05T09:05:00Z",
      "llm_model_version": "gemma4-e4b"
    }
  ]
}
```

---

#### `POST /api/admin/collections/{id}/publish`

컬렉션 공개. status: generating → published, published_at 설정.

**Request Body** (선택)

```json
{ "admin_note": "검수 완료. 주제 적합" }
```

**Response 200**: `{ "id": "uuid", "status": "published", "published_at": "..." }`

**Response 404**: 컬렉션 없음.
**Response 409**: 이미 published 상태.

---

#### `POST /api/admin/collections/{id}/archive`

컬렉션 보류(숨김). status: generating → archived.

**Request Body**

```json
{ "admin_note": "주제 중복으로 보류" }
```

**Response 200**: `{ "id": "uuid", "status": "archived" }`

---

## 6. Frontend Changes

### 6-1. `/explore/collections` 페이지 (신규)

**파일**: `v1/frontend/src/app/explore/collections/page.tsx`

```
레이아웃:
  - 페이지 헤더: "Editor's Pick" (i18n: collections.editors_pick)
  - 부제목: "AI가 큐레이션한 이번 주 주제별 컬렉션" (i18n: collections.subtitle)
  - 컬렉션 카드 그리드 (2열 모바일, 3열 태블릿+)
    ├─ cover_post_id 썸네일 (aspect-ratio 4:3)
    ├─ 컬렉션 제목 (현재 locale 자동 매칭)
    ├─ 설명 2줄 clamp
    ├─ 작품 수 배지 (예: "15 works")
    └─ 주간 날짜 태그 (예: "May 5, 2026")
  - 페이지네이션 (page / limit=10)
```

**API 호출**: `GET /api/collections?locale={currentLocale}&page={page}`

---

### 6-2. `/explore/collections/[id]` 페이지 (신규)

**파일**: `v1/frontend/src/app/explore/collections/[id]/page.tsx`

```
레이아웃:
  - 컬렉션 헤더
    ├─ 대표 이미지 (cover_post_id 썸네일, full-width)
    ├─ 컬렉션 제목 (locale 매칭)
    ├─ 설명 단락
    └─ 공유 버튼 (Twitter/X, Instagram, 링크 복사)
  - OG 메타 태그 (SEO: title, description, og:image)
  - 작품 그리드 (3열, position 순)
    └─ PostCard 컴포넌트 재사용 (기존 컴포넌트)
```

**API 호출**: `GET /api/collections/{id}?locale={currentLocale}`

**OG 메타 (Next.js generateMetadata)**:
```typescript
export async function generateMetadata({ params }: Props) {
  const collection = await fetchCollection(params.id);
  return {
    title: collection.title,
    description: collection.description,
    openGraph: {
      title: collection.title,
      description: collection.description,
      images: [collection.cover_image_url],
    },
  };
}
```

---

### 6-3. i18n (5 locale)

**네임스페이스**: `collections.*` (~10 keys)

| 키 | 한국어 (ko) | 영어 (en) |
|----|------------|---------|
| `collections.editors_pick` | Editor's Pick | Editor's Pick |
| `collections.subtitle` | AI가 큐레이션한 이번 주 주제별 컬렉션 | AI-curated weekly collections |
| `collections.works_count` | {{count}}개 작품 | {{count}} works |
| `collections.week_label` | {{date}} 주간 | Week of {{date}} |
| `collections.share` | 공유하기 | Share |
| `collections.share_link_copied` | 링크가 복사되었습니다 | Link copied |
| `collections.no_collections` | 현재 공개된 컬렉션이 없습니다 | No collections available |
| `collections.view_all` | 모든 컬렉션 보기 | View all collections |
| `collections.back` | 컬렉션 목록으로 | Back to collections |
| `collections.generated_by_ai` | AI 큐레이션 컬렉션 | AI-curated collection |

**파일**: `v1/frontend/src/i18n/{ko,en,ja,zh,es}.json`에 `collections` 키 추가.

---

### 6-4. Sidebar / Explore 메뉴 연결 (선택적)

기존 `Sidebar.tsx`의 Explore 섹션에 "Editor's Pick" 링크 추가.
`/explore/collections` 라우트 연결.

---

## 7. Mock 모드 Fallback

### 7-1. 시나리오별 동작

| 시나리오 | 동작 | 결과 |
|---------|------|------|
| LLM Gateway 미설정 (`LLM_GATEWAY_API_KEY` 없음) | status='generating' 유지, log WARNING | admin이 수동으로 제목/설명 입력 후 publish 가능 |
| sklearn 미설치 | metadata-based grouping (장르 태그 기반) + log WARNING | 클러스터링 품질 저하 가능, 기능은 동작 |
| translation_cache 미가용 | 한국어 원본만 저장, title_translations={}  | 영어 등 번역 없이 한국어만 노출 |
| LLM 비용 한도 초과 | cron skip + log WARNING | 해당 주 컬렉션 미생성 |
| UNIQUE INDEX 충돌 (같은 주제+주) | INSERT 건너뜀 + log INFO | 중복 생성 방지 |
| post_embeddings 부족 (< 5개) | generate 건너뜀 + log WARNING | 클러스터링 최소 데이터 미충족 |

### 7-2. 환경변수 매트릭스

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `AI_CURATION_WORKER_ENABLED` | `true` | `false` 시 cron worker 미등록 |
| `AI_CURATION_DAILY_BUDGET_USD` | `5.0` | LLM 일 비용 한도 (USD) |
| `AI_CURATION_COLLECTIONS_PER_WEEK` | `5` | 주당 생성 컬렉션 수 |
| `AI_CURATION_POSTS_PER_COLLECTION` | `15` | 컬렉션당 포스트 수 |
| `LLM_GATEWAY_API_KEY` | (없음) | 미설정 시 LLM Mock 모드 |

### 7-3. CI/CD 권장 설정

```yaml
# GitHub Actions / docker-compose.test.yml
env:
  AI_CURATION_WORKER_ENABLED: "false"   # CI에서 cron 미시작
  LLM_GATEWAY_API_KEY: ""               # LLM Mock 모드
  AI_CURATION_DAILY_BUDGET_USD: "0"     # 비용 한도 초과 시뮬레이션
```

---

## 8. Cron Worker

### 8-1. 등록 위치

Phase 9 K-5(21번째), Phase 10 K-4(22번째)에 이어 **23번째 cron worker**.

| # | Worker | 주기 | 파일 |
|:-:|--------|------|------|
| 21 | `artwork_caption_worker` | 60초 quick + 24h batch | `ai_caption_jobs.py` (K-3) |
| 22 | `featured_artist_worker` | 주 1회 월요일 06:00 UTC | `featured_artist_jobs.py` (K-4) |
| **23** | **`ai_curation_worker`** | **주 1회 월요일 09:00 UTC** | **`ai_curation_jobs.py` (K-7)** |

**K-4(06:00)와 K-7(09:00) 시간 차이**: Featured Artist 선정(06:00) 후 3시간 뒤에 컬렉션 생성(09:00)하므로
featured artist 작품이 컬렉션에 반영될 수 있다.

### 8-2. 실행 타이밍

```
월요일 06:00 UTC  →  K-4 featured_artist_worker (상위 3명 후보 생성)
월요일 09:00 UTC  →  K-7 ai_curation_worker (5개 컬렉션 생성)
                     ↓
                  admin 검수 큐 확인 (비동기, 언제든)
                     ↓
                  publish → 공개 노출
```

---

## 9. Test Plan

### 9-1. Unit Tests — `tests/unit/test_ai_curation_jobs.py` (8+ tests)

```python
"""Unit tests — ai_curation_jobs.py (Phase 10 K-7)."""
import json
import pytest
from datetime import date
from unittest.mock import AsyncMock, MagicMock, patch


# ──────────────────────────────────────────────────────────────────────────
# T1: 클러스터링 — sklearn 정상 동작
# ──────────────────────────────────────────────────────────────────────────

def test_cluster_by_sklearn_returns_k_groups():
    """sklearn 설치된 경우 k개 클러스터 반환."""
    import numpy as np
    posts = [
        {"post_id": f"p{i}", "embedding": [float(i % 5)] * 128,
         "ai_caption": f"caption {i}", "tags": ["tag"]}
        for i in range(50)
    ]
    from app.services.ai_curation_jobs import _cluster_by_sklearn
    clusters = _cluster_by_sklearn(posts, k=5)
    assert len(clusters) == 5
    total = sum(len(c) for c in clusters)
    assert total == 50


# ──────────────────────────────────────────────────────────────────────────
# T2: 클러스터링 — sklearn 미설치 시 metadata fallback
# ──────────────────────────────────────────────────────────────────────────

def test_cluster_posts_sklearn_fallback(monkeypatch):
    """sklearn ImportError 시 metadata-based grouping으로 전환."""
    import builtins
    real_import = builtins.__import__

    def mock_import(name, *args, **kwargs):
        if name == "sklearn.cluster":
            raise ImportError("sklearn not installed")
        return real_import(name, *args, **kwargs)

    monkeypatch.setattr(builtins, "__import__", mock_import)

    from app.services.ai_curation_jobs import _cluster_posts
    posts = [
        {"post_id": f"p{i}", "embedding": [0.0] * 128,
         "ai_caption": "", "tags": ["painting" if i < 5 else "digital"]}
        for i in range(10)
    ]
    clusters, used_sklearn = _cluster_posts(posts, k=5)
    assert used_sklearn is False
    assert len(clusters) > 0


# ──────────────────────────────────────────────────────────────────────────
# T3: LLM 호출 mock → 제목/설명 생성
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_generate_collection_meta_llm_success():
    """LLM 설정된 경우 제목/설명 반환."""
    from app.services.ai_curation_jobs import _generate_collection_meta

    mock_llm = MagicMock()
    mock_llm.is_mock = False
    mock_llm.generate_interview = AsyncMock(return_value={
        "content": '{"title": "이번 주 신진 페인터", "description": "새로운 작가들이 모였습니다."}',
        "model": "gemma4-e4b",
        "usage_tokens": 100,
    })

    result = await _generate_collection_meta(
        mock_llm,
        captions=["밝은 색채의 추상화", "자연을 모티프로 한 수채화"],
        tags=["painting", "abstract"],
        previous_titles=[],
    )
    assert result is not None
    assert result["title"] == "이번 주 신진 페인터"
    assert "새로운" in result["description"]


# ──────────────────────────────────────────────────────────────────────────
# T4: LLM 미설정 → graceful (None 반환)
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_generate_collection_meta_llm_mock():
    """LLM Gateway 미설정 시 None 반환 (에러 없음)."""
    from app.services.ai_curation_jobs import _generate_collection_meta

    mock_llm = MagicMock()
    mock_llm.is_mock = True

    result = await _generate_collection_meta(
        mock_llm, captions=["테스트"], tags=["tag"], previous_titles=[]
    )
    assert result is None


# ──────────────────────────────────────────────────────────────────────────
# T5: translation_cache 재사용 — cache hit 시 LLM 미호출
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_translate_texts_cache_hit():
    """translation_cache hit 시 LLM generate_interview 호출 없음."""
    from app.services.ai_curation_jobs import _translate_texts

    mock_db = AsyncMock()
    mock_llm = MagicMock()
    mock_llm.is_mock = False
    mock_llm.generate_interview = AsyncMock()

    with patch(
        "app.services.ai_curation_jobs.get_cached_translation",
        return_value="Emerging Painters This Week",
    ):
        title_tr, _ = await _translate_texts(
            mock_db, mock_llm,
            {"title": "이번 주 신진 페인터", "description": ""}
        )

    # cache hit → LLM 번역 호출 없음
    mock_llm.generate_interview.assert_not_called()
    assert "en" in title_tr


# ──────────────────────────────────────────────────────────────────────────
# T6: LLM 비용 한도 초과 시 cron skip
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_ai_curation_cron_skip_on_zero_budget(monkeypatch):
    """AI_CURATION_DAILY_BUDGET_USD=0 이면 generate 호출 없음."""
    monkeypatch.setenv("AI_CURATION_DAILY_BUDGET_USD", "0")

    # cron loop 자체는 asyncio.sleep으로 인해 테스트 불가 → generate 직접 테스트
    from app.services.ai_curation_jobs import generate_collections_for_week

    mock_db = AsyncMock()
    mock_db.execute = AsyncMock(
        return_value=MagicMock(fetchall=lambda: [])
    )

    # posts 부족 → 빈 리스트 반환 (budget 체크는 cron loop에서)
    result = await generate_collections_for_week(mock_db, date(2026, 5, 4))
    assert result == []


# ──────────────────────────────────────────────────────────────────────────
# T7: UNIQUE INDEX 위반 방지 — 같은 주 중복 theme skip
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_generate_skips_duplicate_theme():
    """이미 존재하는 theme+week_start 조합은 INSERT 건너뜀."""
    from app.services.ai_curation_jobs import generate_collections_for_week

    mock_db = AsyncMock()
    # _load_post_embeddings: 충분한 포스트 반환
    # dup_check: existing row 반환 → INSERT 건너뜀
    call_count = 0

    async def mock_execute(sql, *args, **kwargs):
        nonlocal call_count
        call_count += 1
        mock_result = MagicMock()
        sql_text = str(sql)
        if "post_embeddings" in sql_text:
            # 포스트 6개 반환
            rows = [
                MagicMock(
                    post_id=f"p{i}",
                    embedding_text="[" + ",".join(["0.0"] * 128) + "]",
                    ai_caption="caption",
                    tags=["painting"],
                )
                for i in range(6)
            ]
            mock_result.fetchall = lambda: rows
        elif "WHERE theme" in sql_text:
            # 중복 존재
            mock_result.fetchone = lambda: MagicMock(id="existing-id")
        else:
            mock_result.fetchall = lambda: []
            mock_result.fetchone = lambda: None
        return mock_result

    mock_db.execute = mock_execute
    mock_db.commit = AsyncMock()

    with patch("app.services.ai_curation_jobs.LLMGatewayClient") as MockLLM:
        MockLLM.return_value.is_mock = True
        result = await generate_collections_for_week(mock_db, date(2026, 5, 4))

    # 모두 중복 → 생성 없음
    assert result == []


# ──────────────────────────────────────────────────────────────────────────
# T8: post_embeddings 부족 시 빈 리스트 반환
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_generate_skips_when_insufficient_posts():
    """post_embeddings < COLLECTIONS_PER_WEEK 이면 빈 리스트."""
    from app.services.ai_curation_jobs import generate_collections_for_week

    mock_db = AsyncMock()
    mock_result = MagicMock()
    mock_result.fetchall = lambda: []  # 0개
    mock_db.execute = AsyncMock(return_value=mock_result)

    result = await generate_collections_for_week(mock_db, date(2026, 5, 4))
    assert result == []
```

---

### 9-2. Integration Tests — `tests/integration/test_collections_endpoints.py` (5+ tests)

```python
"""Integration tests — /api/collections endpoints (Phase 10 K-7)."""
import pytest
from httpx import AsyncClient


# ──────────────────────────────────────────────────────────────────────────
# T1: GET /api/collections — 공개 목록
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_get_collections_public(client: AsyncClient, db_session):
    """공개 컬렉션 목록 반환. 인증 불필요."""
    response = await client.get("/api/collections?locale=ko")
    assert response.status_code == 200
    data = response.json()
    assert "items" in data
    assert "total" in data
    assert "page" in data


# ──────────────────────────────────────────────────────────────────────────
# T2: GET /api/collections — 페이지네이션
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_get_collections_pagination(client: AsyncClient, db_session, published_collections):
    """limit/page 파라미터 동작 확인."""
    response = await client.get("/api/collections?page=1&limit=2")
    assert response.status_code == 200
    data = response.json()
    assert len(data["items"]) <= 2
    assert data["limit"] == 2


# ──────────────────────────────────────────────────────────────────────────
# T3: GET /api/collections/{id} — 상세 + posts
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_get_collection_detail(client: AsyncClient, db_session, published_collection):
    """컬렉션 상세: posts 포함, position 순 정렬."""
    cid = published_collection["id"]
    response = await client.get(f"/api/collections/{cid}")
    assert response.status_code == 200
    data = response.json()
    assert data["id"] == cid
    assert "posts" in data
    # position 순 정렬 확인
    positions = [p["position"] for p in data["posts"]]
    assert positions == sorted(positions)


# ──────────────────────────────────────────────────────────────────────────
# T4: admin 검수 워크플로우 — publish
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_admin_publish_collection(
    client: AsyncClient, db_session, admin_token, generating_collection
):
    """admin: generating → published 전환."""
    cid = generating_collection["id"]
    response = await client.post(
        f"/api/admin/collections/{cid}/publish",
        headers={"Authorization": f"Bearer {admin_token}"},
        json={"admin_note": "검수 완료"},
    )
    assert response.status_code == 200
    assert response.json()["status"] == "published"

    # 공개 API에서 조회 가능
    pub_response = await client.get(f"/api/collections/{cid}")
    assert pub_response.status_code == 200


# ──────────────────────────────────────────────────────────────────────────
# T5: admin 검수 워크플로우 — archive
# ──────────────────────────────────────────────────────────────────────────

@pytest.mark.asyncio
async def test_admin_archive_collection(
    client: AsyncClient, db_session, admin_token, generating_collection
):
    """admin: generating → archived 전환. 공개 API에서 404."""
    cid = generating_collection["id"]
    response = await client.post(
        f"/api/admin/collections/{cid}/archive",
        headers={"Authorization": f"Bearer {admin_token}"},
        json={"admin_note": "주제 중복으로 보류"},
    )
    assert response.status_code == 200
    assert response.json()["status"] == "archived"

    # 공개 API에서 조회 불가
    pub_response = await client.get(f"/api/collections/{cid}")
    assert pub_response.status_code == 404
```

---

## 10. KPI 측정

### 10-1. 측정 항목별 방법

| KPI | 목표 | 측정 도구 | 측정 방법 |
|-----|------|---------|---------|
| 컬렉션 클릭률 | ≥ 15% | PostHog | `collection_click` 이벤트 / `/explore` 방문 수 |
| admin 승인율 | ≥ 70% | DB 쿼리 | `COUNT(published) / COUNT(published + archived)` |
| LLM 비용 | 일 ≤ $5 | tuzigroup 사용량 API | 일별 token 소비 × 단가 |
| translation_cache 히트율 | ≥ 60% | DB 집계 | `hit_count > 0` 행 비율 |
| 신진작가 발굴 가속 | 후원 전환율 baseline | PostHog | `sponsor_created` funnel (컬렉션 진입 사용자 코호트) |

### 10-2. PostHog 이벤트 정의

```typescript
// 컬렉션 클릭 (/explore/collections 페이지에서)
posthog.capture("collection_click", {
  collection_id: id,
  collection_theme: theme,
  locale: currentLocale,
  source: "explore_page",  // "explore_page" | "sidebar" | "feed_card"
});

// 컬렉션 내 작품 클릭
posthog.capture("collection_post_click", {
  collection_id: id,
  post_id: postId,
  position: position,
});

// 공유 버튼 클릭
posthog.capture("collection_share", {
  collection_id: id,
  platform: "twitter" | "instagram" | "copy_link",
});
```

---

## 11. 위임 Agent

| Agent | 담당 작업 | 주요 산출물 |
|-------|----------|-----------|
| `bkend-expert` | alembic 0083 작성 + `ai_curation_jobs.py` 전체 + 5개 API 엔드포인트 + `main.py` 23번째 cron 등록 + unit/integration tests | `0083_ai_collections.py`, `ai_curation_jobs.py`, `app/api/collections.py`, `tests/unit/test_ai_curation_jobs.py`, `tests/integration/test_collections_endpoints.py` |
| `frontend-architect` | `/explore/collections` 페이지 + `/explore/collections/[id]` 페이지 + i18n 5 locale (10 keys) + OG 메타 | `app/explore/collections/page.tsx`, `app/explore/collections/[id]/page.tsx`, `i18n/*.json` 업데이트 |

### 병렬 실행 범위

```
[병렬]
  bkend-expert: alembic 0083 + ai_curation_jobs.py + API routes
  frontend-architect: /explore/collections 페이지 skeleton + i18n 키 추가

[순차 — bkend 완료 후]
  frontend-architect: API 연동 (GET /api/collections, GET /api/collections/{id})
  bkend-expert: unit tests 작성 + 통과

[검증]
  integration tests green + tsc 0 errors + alembic upgrade head green
```

---

## 12. 위험 & 완화

| 위험 | 영향 | 가능성 | 완화 |
|------|:----:|:------:|------|
| K-means 클러스터링 품질 저하 → 주제 없는 혼합 컬렉션 | 높음 | 중간 | admin 검수(autopublish OFF)로 품질 안전망. k=5 고정값 → admin 거부율 모니터링 후 k 조정 |
| LLM 비용 폭증 | 높음 | 낮음 | 일 $5 hard cap + translation_cache 재사용 + cron 실패 시 skip (무한 재시도 X) |
| LLM 제목 클리셰 반복 ("이번 주 신진 페인터") | 중간 | 중간 | previous_titles 프롬프트 포함 → 다양성 주입. theme 식별자 8개 로테이션 |
| post_embeddings 벡터 품질 저하 (Mock 모드) | 중간 | 낮음 | Mock 모드 zero vector → 클러스터링 무의미 → metadata fallback으로 대체 |
| alembic 0083 / K-4 0082 충돌 | 중간 | 낮음 | down_revision 명시(0082_featured_artist_candidates) + Wave B 병렬 진행 후 순차 merge |
| admin 검수 미완료로 컬렉션 적체 | 낮음 | 중간 | 48h 미검수 시 Slack 알림 (L-F cohort_alert 패턴 재사용) |
| `/explore/collections` 빈 상태 (초기) | 낮음 | 높음 | 빈 상태 UI 처리 (`collections.no_collections` i18n 키) |

---

## Version History

| 버전 | 날짜 | 변경사항 | 작성자 |
|------|------|---------|--------|
| 0.1 | 2026-05-05 | Phase 10 K-7 초기 design 작성. alembic 0083, ai_curation_jobs.py 전체 구현, 5 API endpoints, 8 unit tests + 5 integration tests, Frontend 2페이지 + i18n 10 keys. | itpe-ince (Claude Sonnet 4.6) |
